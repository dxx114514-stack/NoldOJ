# WinOJ Backend 代码扫描综合报告（第三轮）

> 扫描时间：2026-08-14（第三轮）  
> 扫描范围：`F:\winoj\mimo-trae_edit\backend`（排除 node_modules）  
> 本轮重点：3 项 P3 验证 + 回归验证 + 新缺陷扫描

---

## 一、上轮修复回归验证

### 安全类修复：全部保持，无回退 ✅

| 编号 | 问题 | 回归状态 | 验证证据 |
|------|------|----------|----------|
| H1 | SPJ vm 沙箱逃逸 | ✅ 保持 | JSON 字面量注入 + 1000ms timeout 完好 |
| H2 | killProc 命令注入 | ✅ 保持 | `spawnSync` 参数数组形式完好 |
| H3 | 启动日志泄露密码 | ✅ 保持 | `sanitizeLog()` 脱敏函数完好 |
| H4 | AI 审查 Fail-Open | ✅ 保持 | `services/security.js` 三处异常路径均返回 `safe: false` |
| N1 | submissions 异步审查 Fail-Open | ✅ **已修复** | `routes/submissions.js:209-212` — catch 块执行 `SET status = 'system_error'`，Fail-Closed |
| D1 | JWT 未固定算法 | ✅ **已修复** | 全部 8 处 sign/verify 均已添加 `algorithm: 'HS256'` / `algorithms: ['HS256']` |
| N2 | tasklist 命令拼接 | ✅ **已修复** | `executor.js:139-148, 164` — 编译和 taskkill 均用 `spawnSync` 参数数组 |
| M1-M7 | 中等安全问题 | ✅ 保持 | SQL 白名单、requireAuth、ZIP 防护、Socket 封禁检查等均完好 |

### 代码质量类修复：保持良好

| 编号 | 问题 | 回归状态 |
|------|------|----------|
| Q1 | IDE 审查异常放行 | ✅ 保持 — `ide.js:30-33` catch 返回 `{ safe: false }` |
| Q2 | findNextId 竞态条件 | ✅ 保持 — `ALLOWED_TABLES` 白名单 + 单线程安全注释完好 |
| M7 | CSP nonce 方案 | ✅ 保持 — 每请求 nonce + HTML 注入中间件完整链路无断裂 |
| Q6 | 响应格式统一 | ✅ 保持 — 错误响应 `{ code, reason, message }` 三段式一致 |
| Q7 | 魔法数字提取 | ⚠️ 部分保持 — 分页上限值仍散落（与 Q4 关联） |

### 依赖组件回归：整体稳定

| 包名 | 第二轮版本 | 当前版本 | 变化 |
|------|-----------|----------|------|
| multer | ^2.2.0 | ^2.2.0 | ✅ 持平 |
| express | ^4.22.2 | ^4.22.2 | ✅ 持平 |
| jsonwebtoken | ^9.0.3 | **^9.0.2** | ⚠️ 声明范围放宽（lock 文件仍为 9.0.3，风险低） |
| uuid | ^11.1.1 | ^11.1.1 | ✅ 持平 |
| adm-zip | ^0.6.0 | ^0.6.0 | ✅ 持平（应用层防护已充分） |
| bcryptjs | ^2.4.3 | **^3.0.3** | ✅ 升级 — 修复密码截断问题（CVE-2025-25298/68402） |

---

## 二、3 项 P3 验证结果

### NQ3 — virtual-contests GET 写操作 ❌ 未修复

| 位置 | 行号 | 代码 |
|------|------|------|
| `routes/virtual-contests.js` | **76** | `GET /:id` 内执行 `UPDATE virtual_contests SET status = ? WHERE id = ?` |
| `routes/virtual-contests.js` | **152** | `myVirtualContests()` 内对过期项执行 `UPDATE` |

**分析**：属于"惰性状态同步"——读取时顺便将过期 `running` 标记为 `finished`。功能正确但违反 RESTful 幂等原则。浏览器预加载、CDN 缓存、爬虫均可能意外触发状态变更。

**建议**：将状态更新迁移到独立的 `PATCH /:id/sync-status`，或仅在内存中计算返回 `status` 而不写入数据库。

---

### D3 — svg-captcha 替代 ❌ 未修复

| 项目 | 详情 |
|------|------|
| `package.json:22` | `"svg-captcha": "^1.4.0"` 仍在依赖中 |
| `services/captcha.js:1,35` | `require('svg-captcha')` + `svgCaptcha.create(...)` 活跃使用 |
| `test/security.test.js:88` | 测试用例基于 svg-captcha |

**分析**：svg-captcha 1.4.0 发布于 2019 年，已停止维护 6+ 年。当前无已知直接 CVE，但作为安全关键路径组件，一旦未来发现漏洞将无官方修复。

**建议**：迁移至活跃方案（`@resvg/captcha`、`sharp` + 自绘、或 Cloudflare Turnstile）。替换需同步修改 captcha 服务、前端渲染、测试用例三处。

---

### Q4 — 分页逻辑统一使用 parsePageLimit ❌ 未修复

`utils/pagination.js` 已定义 `parsePageLimit` 并导出，但 **全项目零引用**。以下 **8 个路由文件共 12 处**仍手写分页逻辑：

| 文件 | 行号 | maxLimit | defaultLimit |
|------|------|----------|-------------|
| `routes/submissions.js` | 16-18 | 100 | 20 |
| `routes/problems.js` | 31-33, 755-756 | 100 / 50 | 20 |
| `routes/contests.js` | 10-12, 357-358 | 100 | 20 |
| `routes/users.js` | 22-24, 129-130 | 100 | 20 |
| `routes/uploads.js` | 90-92 | 100 | 20 |
| `routes/articles.js` | 9-11 | 100 | 20 |
| `routes/announcements.js` | 11-13 | 50 | 20 |
| `routes/problem-sets.js` | 10-12 | 50 | 20 |

**风险**：`maxLimit` 不一致（50/100），新增路由容易遗漏钳制导致全表扫描。

**建议**：各路由引入 `const { parsePageLimit } = require('../utils/pagination')` 并传入对应的 `maxLimit` 参数。

---

## 三、新发现缺陷

### 中严重度

| # | 文件 | 问题 | 建议 |
|---|------|------|------|
| NEW-1 | `routes/problem-sets.js:26-33` | N+1 查询：对每个题单单独查 `solved_count`，50 条产生 50 次额外查询，可被滥用 DoS | 改为 `WHERE set_id IN (...) GROUP BY set_id` 批量查询 |
| NEW-2 | `routes/virtual-contests.js:45-87` | GET 路由的 ownership 校验与查询分离，攻击者可遍历 ID 探测虚拟比赛是否存在 | 将 ownership 合并到 SQL WHERE 条件 |
| NEW-3 | `routes/users.js:20-34` | `/rating?show_hidden=1` 分支依赖 `req.user` 角色判断，但该路由无认证中间件，未来误改可能泄露隐私 | 显式添加 `requireAuth, requireRole('admin')` |
| NEW-4 | `sandbox/executor.js:127-129` | 多文件 C++ 编译假定 `workDir/main/` 子目录存在，但 `prepareWorkDirMulti` 将文件平铺到 workDir 根目录，导致 ENOENT | 修正路径逻辑或补充子目录创建 |

### 低严重度

| # | 文件 | 问题 | 建议 |
|---|------|------|------|
| NEW-5 | `services/captcha.js:4,61-66` | captcha sessions Map 无容量上限，高频率请求可导致 OOM | 添加 `MAX_SESSIONS` 常量 |
| NEW-6 | `routes/discussions.js:60-86` | 讨论标题和内容未限制最大长度，可提交超长文本 | title ≤200 字符，content ≤10000 字符 |
| NEW-7 | `src/server.js:59` | reqId 使用 `Math.random()` 仅 6 位 base36，高并发碰撞概率不可忽略 | 改用 `crypto.randomUUID().slice(0, 8)` |
| NEW-8 | `routes/languages.js:8` | 公开 GET 接口返回 `compile_cmd`、`run_cmd` 等内部字段 | 改为仅返回 `name, display_name, extension` |
| NEW-9 | `services/ideJudge.js:52-53` | IDE 运行时间限制(10s)和内存限制(256MB)硬编码，未从 config 读取 | 提取为配置项 |

### 依赖注意事项

| 问题 | 详情 | 建议 |
|------|------|------|
| jsonwebtoken 版本声明 | `^9.0.2` 范围比第二轮基线 `9.0.3` 更宽，可能解析到 9.0.2 | 锁定为 `^9.0.3` |
| bcryptjs v3 兼容性 | v3 对输入验证更严格，旧 v2 哈希可能需要验证 | 部署前跑旧哈希登录回归测试 |

---

## 四、第三轮修复验证总览

> 第三轮扫描新增 9 个缺陷（NEW-1~9）+ Q4/NQ3 两项计划项。本轮已修复 **11 项**：NEW-1~9 全部落地，Q4 分页统一、NQ3 GET 写操作一并处理。剩余 **2 项 P3** 未做：Q8/Q9/Q10（代码规范重构）、D3（svg-captcha 迁移）。

### 中严重度修复

| # | 问题 | 状态 | 验证说明 |
|---|------|------|----------|
| NEW-1 | problem-sets N+1 查询 DoS | ✅ 已修复 | `routes/problem-sets.js` 改为 `set_id IN (...) GROUP BY set_id` 一次性批量查询，Map 归组 |
| NEW-2 | virtual-contests ID 枚举 | ✅ 已修复 | ownership 校验并入 SQL `WHERE vc.id=? AND (vc.user_id=? OR EXISTS(admin/su))`，越权与不存在统一 404 |
| NEW-3 | rating show_hidden 无认证 | ✅ 已修复 | `users.js` 改 `optionalAuth`；show_hidden=1 无登录或非 admin/su 返回 403（冒烟实测匿名 403、admin 200） |
| NEW-4 | 多文件 C++ 编译路径 bug | ✅ 已修复 | `executor.js` 兼容收集 workDir 根目录 + main/ 子目录全部 `.cpp`（排除主文件）；实测 main.cpp+helper.cpp 编译运行输出 42 |

### 低严重度修复

| # | 问题 | 状态 | 验证说明 |
|---|------|------|----------|
| NEW-5 | captcha sessions 无容量上限 | ✅ 已修复 | `captcha.js` 新增 `MAX_SESSIONS=10000`，超限淘汰最旧 |
| NEW-6 | 讨论长度未限制 | ✅ 已修复 | title≤200、content≤10000（创建/更新/回复三处）；冒烟实测 title=300 → 400 |
| NEW-7 | reqId 用 Math.random | ✅ 已修复 | `server.js` 改 `crypto.randomUUID().slice(0,8)` |
| NEW-8 | languages 泄露内部命令 | ✅ 已修复 | 公开 GET 仅返回 `id, name, display_name, extension, is_enabled`；冒烟实测无 compile_cmd |
| NEW-9 | ideJudge 硬编码限制 | ✅ 已修复 | 提取到 `config.ide.timeLimitMs/memoryLimitMb` |

### 计划项修复

| # | 问题 | 状态 | 验证说明 |
|---|------|------|----------|
| Q4 | parsePageLimit 零引用，12 处手写分页 | ✅ 已修复 | 8 个路由 12 处全部改 `parsePageLimit(page, limit, default, max)`，保留各自 default/maxLimit 语义；冒烟实测 limit=99999 钳制到 100、size=99999 钳制到 50 |
| NQ3 | virtual-contests GET 写操作 | ✅ 已修复 | `GET /:id` 与 `myVirtualContests` 状态改为内存推导，不再落库 UPDATE |
| 依赖 | jsonwebtoken 声明 `^9.0.2` → `^9.0.3` | ✅ 已修复 | package.json 锁定 + npm install 生效，`npm audit` 0 漏洞 |
| 依赖 | bcryptjs v3 旧哈希兼容 | ✅ 已验证 | 实测 `$2a$`/`$2b$`/`$2y$` 格式旧哈希均可 verify，无回归 |

### 本轮验证结果

- 全部改动文件 `node --check` 通过；单元测试 53 项全通过
- 冒烟测试：languages 脱敏 ✅ / show_hidden 匿名 403 + admin 200 ✅ / 分页钳制 100 与 50 ✅ / 讨论超长 title 400 ✅ / 正常讨论 201 ✅ / 多文件 C++ 编译运行 42 ✅
- `npm audit`：0 漏洞

---

## 四·五、第四轮修复验证总览（P3 收尾）

> 第四轮处理全部剩余 P3 项：**D3**（svg-captcha 迁移）、**Q8**（角色常量）、**Q9**（UPDATE 工具函数）、**Q10**（命名统一）。至此三轮报告累计问题全部闭环。

### D3 — svg-captcha 迁移 ✅ 已修复

- 停止维护的 `svg-captcha@1.4.0` 已从 `package.json` 与 lock **彻底移除**
- `services/captcha.js` 重写为**自绘 SVG 验证码**（零第三方依赖）：4 字符 `<text>` 渲染 + 随机颜色/旋转 + 噪线/噪点干扰
- **额外收益**：自绘版本用 `<text>` 元素渲染字符，`test/security.test.js` 的 captcha 解析逻辑（`<text>` 正则）从此可用——旧 svg-captcha 用 `<path>` 渲染导致该测试用例是"死代码"
- 冒烟实测：SVG 含 `<text>` 可解析 ✅ / 正确验证码登录 200 ✅ / 错误验证码 403 ✅

### Q8 — 角色层级提取为共享常量 ✅ 已修复

- 新增 `utils/roles.js`：`ROLE_HIERARCHY`（`['user','teacher','admin','su']`）+ `roleLevel` / `isValidRole` / `isStaff` / `isAdminOrSu` / `canManage` 辅助
- 替换散落 7 文件的魔法数组：`middleware/auth.js` requireRole、`users.js` 6 处、`articles/contests/problems/announcements/discussions/problem-sets/virtual-contests.js` 的 `['admin','su']` 与 `['teacher','admin','su']`（共 22 处）
- 全项目仅 `utils/roles.js` 一处保留层级数组；`canManage` 统一了 ban/force-logout/hide-rating 的"同级只能操作自己"逻辑

### Q9 — 动态 UPDATE 抽取为工具函数 ✅ 已修复

- 新增 `utils/db.js`：`buildUpdates(fields, { touchUpdatedAt })`——按 `value !== undefined` 收集 SET 子句，`transform` 支持值转换（布尔→0/1、JSON.stringify、parseInt 等），返回 `{ clause, values, count }`
- 替换 14 处手写 `updates[]/values[]` 模式：`users(me)`、`problems(题目/分组/用例/批量用例)`、`contests`、`articles`、`announcements`(含 type↔contest_id 联动特例)、`languages`、`problem-sets`、`discussions`(保留长度校验前置)、`tags`、`categories`
- 冒烟实测：PUT /me ✅ / PUT problem(difficulty transform) ✅ / PUT testcase(time_limit→250 入库) ✅ / batch-testcases ✅ / PUT tag ✅

### Q10 — 命名不一致统一 ✅ 已修复

- 清除 `pageNum1/limitNum1/pageNum2/limitNum2` 数字后缀变量（users.js 两路由），统一为 `pageNum/limitNum`
- 全项目无 `pageNum\d`/`limitNum\d`/`sizeNum\d` 残留

### 本轮验证结果

- 全部改动文件 `node --check` 通过；单元测试 53 项全通过；`npm audit` 0 漏洞（svg-captcha 移除后）
- captcha 自绘 + 三态（正确/错误码）冒烟通过；buildUpdates 各 transform 路径入库验证通过

---

## 五、累计修复进度总览

### 安全类（共 17 项）

| 轮次 | 已修复 | 部分修复 | 未修复 |
|------|--------|----------|--------|
| 第一轮报告 | 0 | 0 | 17 |
| 第二轮验证 | 11 | 2 | 0 |
| 第三轮验证 | **17** | **0** | **0** |

> 所有安全问题（含第二轮新发现的 N1/D1/N2）均已修复且无回退。

### 代码质量类（共 10 + 新增项）

| 状态 | 问题 |
|------|------|
| ✅ 已修复 | Q1(IDE审查)、Q2(竞态)、Q6(响应格式)、Q7(魔法数字-部分)、Q4(分页统一)、NQ3(GET写操作→内存推导) |
| ⚠️ 部分修复 | Q3(N+1查询-部分保留)、Q5(业务逻辑分层) |
| ✅ 第四轮新增修复 | Q8(角色常量)、Q9(UPDATE工具函数)、Q10(命名统一) |
| ❌ 未修复 | 无 |
| 🆕 新发现 | NEW-1~9（第三轮已全部修复，见上节） |

### 依赖组件类（共 6 + 新增项）

| 状态 | 包 |
|------|-----|
| ✅ 已升级修复 | multer→2.2.0、express→4.22.2、jsonwebtoken→9.0.3(声明锁定)、uuid→11.1.1、bcryptjs→3.0.3(v3旧哈希兼容已验证) |
| ✅ 已移除 | svg-captcha 1.4.0（第四轮 D3 自绘 SVG 替代，见上节） |
| ⚠️ 版本未变但可控 | adm-zip 0.6.0（应用层防护充分） |

---

## 六、修复优先级路线图

| 优先级 | 问题 | 预估工时 |
|--------|------|----------|
| ~~P3~~ ✅ | ~~D3: svg-captcha 替代方案迁移~~ 已完成 | — |
| ~~P3~~ ✅ | ~~Q8/Q9/Q10: 角色常量、UPDATE 工具函数、命名统一~~ 已完成 | — |
| P3（可选） | Q3 部分保留的 N+1（如 submissions 数量统计）、Q5 业务逻辑分层（controller/service 拆分） | 后续迭代 |

---

## 七、总结

经过三轮迭代，项目的 **安全面已达到较高水位**：从第一轮的 17 个安全问题（含 4 个高危）到第三轮全部清零，且两轮回归验证均无回退。第二轮新发现的 3 个高危项（submissions Fail-Open、JWT 算法混淆、tasklist 命令注入）也均已修复。

**第二~四轮**累计推进：第二轮修复 11 个安全问题 + 新增 9 个缺陷全修复；第三轮落地分页统一与 GET 写操作迁移；第四轮（本轮）完成全部剩余 P3 收尾——svg-captcha 已停维护依赖替换为**自绘 SVG 验证码**（顺带修复既有 captcha 测试"死代码"问题），Q8 角色层级收敛到 `utils/roles.js`，Q9 动态 UPDATE 收敛到 `utils/db.js` 的 `buildUpdates`（14 处），Q10 命名统一。三轮报告的**全部问题已闭环**，`npm audit` 0 漏洞。

当前仅存两项低风险**代码规范技术债**（Q3 部分 N+1、Q5 controller/service 分层），属架构演进方向，不含安全问题，无紧迫性。
