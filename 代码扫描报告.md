# WinOJ 代码扫描报告（第五轮 · 三维度全量扫描）

> 扫描时间：2026-08-14（第五轮）  
> 扫描范围：`backend/`、`frontend/`、`scripts/`、`backend/sandbox/sandbox_runner.cpp`（排除 node_modules/.git）  
> 扫描维度：**代码缺陷 / 代码规范 / 悬镜组件（SCA）分析**  
> 代码规模：85 个源文件、约 1.4 万行

---

## 〇、执行摘要

| 维度 | 高危 | 中危 | 低危/规范 | 备注 |
|------|------|------|----------|------|
| 代码缺陷 | 2 | 5 | 9 | 2 个高危（ZIP 路径穿越、多文件编译参数注入） |
| 代码规范 | — | — | 36 项 | 死代码 6 处、重复逻辑 9 组、日志绕过 4 类 |
| 悬镜组件 | 0 CVE | 0 CVE | 4 项 | `npm audit` 0 漏洞，剩余为维护性建议 |

上轮（第四轮）修复的 D3/Q8/Q9/Q10 等全部保持，`npm audit` 仍为 0 漏洞。本轮重点报告 **2 个高危缺陷**与 **组件维护性建议**。

---

## 一、代码缺陷扫描（Code Defect）

### 高危（High）

| # | 位置 | 缺陷 | 危害 | 建议 |
|---|------|------|------|------|
| D-H1 | `routes/problems.js:338-343, 416-419, 429-432` | **ZIP 测试数据导入路径穿越**。仅用 `entryPath.split('/')` 校验 `..` 与 `path.isAbsolute`，未将 `\` 归一化。条目名 `..\..\evil.in` 时 `parts=['..\..\evil.in']` 通过校验，`name` 未消毒直接 `path.join(problemDir, name)`，Windows 下写出 problemDir | 教师角色可将任意 `.in/.out/.txt` 内容写到服务器任意磁盘路径（受 JSON 注册影响，可写至项目根外） | `entryPath` 先 `.replace(/\\/g,'/')`；对 `subtaskName`/`name` 做 `path.basename` 白名单（仅 `[\w-]+`）；写入前用 `path.resolve(problemDir)+path.sep` 前缀断言 |
| D-H2 | `sandbox/executor.js:126-149` | **多文件 C++ 编译参数注入**。`extraSources` 以文件名拼进命令行再按空白 token 化，文件名含空格（`x -o ..\evil.cpp` 等）会变成 gcc 额外参数（重复 `-o` 重定向产物、`-wrapper/-B/-include` 等） | 编译在沙箱外以服务器身份执行，可把产物写出 workDir / 影响编译行为 | 编译改用 `spawnSync(compiler, [...基准参数, ...extraSources, srcFile])` 数组直传；或在判题侧拒绝含空白/前导 `-` 的文件名 |

### 中危（Medium）

| # | 位置 | 缺陷 | 建议 |
|---|------|------|------|
| D-M1 | `services/email.js:45,51` | **邮箱验证码 10 分钟有效期失效**：存储 ISO 格式 `…T…Z`，比较用 `datetime('now')` 空格格式，同日内恒 `>` 恒真 → 验证码当日不失效（最多约 24h） | 存 `datetime('now','+10 minutes')`，或比较 `datetime(expires_at) > datetime('now')` |
| D-M2 | `services/judge.js:168-169, 202-203` | **判题输入输出整文件读入内存**，无单文件大小上限（ZIP 500MB 总预算内单文件可数百 MB）→ 判题进程 OOM | 判题前 `fs.statSync` 限制单文件（如 16MB），超大用例改用流式/特判 |
| D-M3 | `routes/submissions.js:162,181-183` + `services/judge.js` | **`submit_answer` 题型未实现判题**：`answer_data` 入库后从未被读取比较，提交即判 `compile_error` | judge 中对 `problem_type==='submit_answer'` 短路：`compareOutput(expected, answer_data)` 直接评分 |
| D-M4 | `routes/contests.js:271-274`、`virtual-contests.js:115-117` | **排行榜 AC 可能被同分更快的 WA 顶掉**：选优仅比较 score+time，未优先 `accepted`，导致已 AC 的题记 0 分 | 比较优先级改为 `status==='accepted'` 优先 → 再比 score → 再比 time |
| D-M5 | `routes/problems.js:600` | **删除测试组用字符串 `REPLACE` 改 dependency JSON**：`REPLACE(...,'12','')` 会误伤 `112`，残留 `[1,]` 非法 JSON，`JSON.parse` 失败使依赖静默失效 | 读出 → `JSON.parse` → filter → 重新 stringify → 参数化 UPDATE |

### 低危（Low）

| # | 位置 | 缺陷 | 建议 |
|---|------|------|------|
| D-L1 | `routes/uploads.js:27` + `src/server.js:165` | **上传白名单含 `js` 且经 `/uploads` 静态服务直接输出**（`text/javascript`、无 nosniff/attachment）。当前页面渲染已转义/DOMPurify，暂无直接注入点，但属纵深防御缺口，一旦任一渲染面放开则变为存储型 XSS（`access_token` 存于 localStorage，风险放大） | 移除 `js` 扩展；`/uploads` 静态服务加 `nosniff` + 尽量 `attachment` |
| D-L2 | `services/judge.js:176-180, 209-213` | **非零退出码 + 输出正确 = AC**：仅当输出不匹配才升级为 RE | 输出正确但 `exitCode!==0` 也应判 `runtime_error`（除非题型有约定） |
| D-L3 | `routes/submissions.js:294-296` + `services/judge.js:500-505` | **重测已 AC 提交可能重复发放 Rating(+10)**（`prevAccepted` 只看 `id <` 的其它提交） | 仅当该提交此前非 `accepted` 时才发放 |
| D-L4 | `routes/contests.js:256-263` | **榜单包含未参赛用户**（未 join `contest_participants`） | 加 `s.user_id IN (SELECT user_id FROM contest_participants WHERE contest_id=?)` |
| D-L5 | `routes/users.js:66` | **`signature` 非字符串时 `.slice` 抛 500**（`PUT /me`） | `String(v ?? '').slice(0,1000)` 先强转 |
| D-L6 | `routes/users.js:289-290` + schema | **删除有内容用户触发 FK 约束 → 500**，删除静默失败（`submissions/articles/discussions` 无级联） | 事务内先清理/匿名化依赖数据，或 catch FK 返回友好 400 |
| D-L7 | `routes/problems.js:269-294` | **testdata 批量上传中途失败遗留 multer 临时文件**（无 try/finally 清理） | try/catch/finally 中删除所有 `req.files` |
| D-L8 | `src/server.js:294-317` | **uncaughtException/unhandledRejection 仅记日志不退出**；shutdown 无强制超时；DB 句柄从未 close | 致命错误 `process.exit(1)`；shutdown 加超时强退；导出并调用 `db.close()` |
| D-L9 | `services/judge.js:533-566` | **内存判题队列重启后丢失**：pending/running 提交重启后永久卡死（提交锁连带阻塞） | 启动时扫描 `pending/pending_rejudge/pending_review` 并重置中断态重新入队 |

### 已确认安全（排除项，不报）

- `findNextId` 表名白名单 + 同步单线程无竞态；refresh token 原子消费；SQL 全参数化；like 转义 `%`/`_`/`\`；SPJ vm 仅注入 JSON 字面量；captcha/ratelimit 均有界 + 定时清理；`parsePageLimit` 钳制正常。

---

## 二、代码规范扫描（Code Standard）

### 死代码 / 冗余（可立即删除）

| # | 位置 | 问题 |
|---|------|------|
| S-1 | `routes/uploads.js:63-75` | `createUploadMiddleware` 定义了但从未使用（实际用静态 `upload`） |
| S-2 | `database/db.js:43,354` | `saveDB(){}` 空壳导出，无调用 |
| S-3 | `services/socket.js:91-93,130` | `getIO()` 导出零调用 |
| S-4 | `routes/problems.js:27-30` | `fullProblem(p)` 恒等包装冗余 |
| S-5 | `routes/uploads.js:94-97` | `role==='user'` 分支不可达（路由已 `requireRole('teacher')`） |
| S-6 | `routes/announcements.js:18-22` | `where` 重复赋值同一字面量 |

### 重复逻辑（应抽共享函数）

| # | 位置 | 问题 |
|---|------|------|
| S-7 | `socket.js:22-49` vs `middleware/auth.js:13-40` | Socket JWT 认证与 HTTP `requireAuth` 双份实现，改一处易漏一处 |
| S-8 | `auth.js:88-96, 127-135` | login/register 验证码块重复 → 抽 `requireCaptcha()` |
| S-9 | `ide.js:25-28, 56-59`、`submissions.js:200-203` | 「恶意代码→封禁+强制登出+删 token」重复 3 处 → 抽 `banUserAndRevoke()` |
| S-10 | `auth.js:21-27` vs `users.js:219,258` | 密码强度校验三份实现 → 移入 `utils/validation.js` |
| S-11 | `users.js:246-248` vs `auth.js:29-31` | AccessToken 签发逻辑第三份内联副本（含函数内 `require`）→ 抽 `utils/tokens.js` |
| S-12 | `judge.js:163-190` vs `196-223` | 分组/非分组两路每用例跑分循环近 30 行重复 → 抽 `runSingleTestCase()` |
| S-13 | `server.js:136-150` vs `245-257` | nonce 注入 regex 重复 → 抽 `injectNonce()` |
| S-14 | `auth.js:108-113, 176-181, 231-236` | refresh cookie 设置重复 3 处 → 抽 `setRefreshCookie()` |
| S-15 | `submissions.js:66-103` vs `219-242` | `GET /:id` 与 `/:id/detail` 三连查重复 → 合并 handler |

### 错误处理 / 响应规范不一致

| # | 位置 | 问题 |
|---|------|------|
| S-16 | `auth.js:43` | 全项目唯一不返回 `{code,reason,message}` 封装的端点（`{error:...}`），前端需特判 |
| S-17 | `tags.js:24` vs `categories.js:32` vs `languages.js:21` | 同一「重复资源」三种状态码（409/400/400）+ 三种 reason |
| S-18 | 分页响应字段名混用 | 部分路由 `size`、部分 `limit`（announcements/problem-sets/discussions vs articles/problems/submissions/users）→ 统一 `page`+`limit` |
| S-19 | `users.js:165`、`problems.js:216,249` | 同类「不能操作」case 用 400/ERR_INVALID_STATE 与 403/ERR_FORBIDDEN 混用 |

### 日志规范（绕过集中脱敏 logger）

| # | 位置 | 问题 |
|---|------|------|
| S-20 | `submissions.js:199` | `console.log(result.reason)` 未脱敏（AI 审查 reason 可能含攻击者内容） |
| S-21 | `users.js:15` | `audit()` 裸 `console.log`，操作者/目标名未 sanitizeLog |
| S-22 | `problems.js:443` | `console.error('[problems] ZIP import failed:', err)` 未脱敏 |
| S-23 | security.js/email.js/plagiarism.js/ideJudge.js/judge.js/socket.js/db.js | 约 10 处裸 `console.*` 未走 `log/logInfo/logError` |

### 魔法数字 / 风格

| # | 位置 | 问题 |
|---|------|------|
| S-24 | `judge.js:497,505` | Rating 增减 `-2/+10` 硬编码 |
| S-25 | `judge.js:183,184,216,217` | `slice(0,4096)` 重复 4 次 |
| S-26 | `judge.js:342`、SPJ `timeout:1000`、`server.js:289` | 循环上限 `100`、SPJ 超时、内存轮询等无名字 |
| S-27 | 多文件 | 函数内 `require()`（users.js:58,246、problems.js:760,768、contests.js:309,333,345）→ 提升到模块顶部 |
| S-28 | `server.js:29-32` | `logStartup` 与 `logInfo` 字节相同 |
| S-29 | `submissions.js:111,196-217` vs `ide.js:52-66` | 同类 AI 审查一处 fire-and-forget、一处 await，并发风格不统一 |
| S-30 | `virtual-contests.js:46` | 路由声明 `async` 但从不 await |
| S-31 | `middleware/auth.js:66` vs `:11` | 在线用户 TTL `5*60*1000` 双处出现 |

### 前端

| # | 位置 | 问题 |
|---|------|------|
| S-32 | `login.html:49-67` / `register.html:64-96` | loadCaptcha 约 20 行重复 → 移入 api.js 共享 |
| S-33 | `api.js:83-89, 105-111, 116-125` | `refreshNav`/`refreshUser`/`refreshRating` 三处重复用户刷新逻辑 |
| S-34 | `api.js:398,402,406,412,435` | 生产环境 socket console.log/warn |
| S-35 | `index.html:18,21,25,32,39,48-51` | Tailwind 暗色类重复，可抽 `.stat-card` |
| S-36 | 各页面 | 内联 `onkeyup/onchange/onsubmit` 与 api.js 的 addEventListener 风格混用 |

> 前端脚本引用核验：所有 `<script src>` 均能解析（仅 `/js/api.js`、`/js/markdown.js`），30 页 favicon/viewport/nav 齐全，无死引用。

---

## 三、悬镜组件（SCA）分析

### 3.1 漏洞审计

- **`npm audit`：0 漏洞**（critical/high/moderate/low 全为 0），126 个依赖（prod 125 + peer 1）。
- 上轮已升级项（bcryptjs→3.0.3 修复 CVE-2025-25298/68402、jsonwebtoken 声明锁定 9.0.3、multer→2.2.0）保持无回退。
- 已移除停维护的 `svg-captcha@1.4.0`（2019 年停更），改自绘 SVG，无第三方依赖。

### 3.2 组件维护性 / 版本健康度

| 组件 | 当前 | npm 最新 | 状态 / 建议 |
|------|------|----------|-------------|
| express | 4.22.2 | 5.2.1 | ⚠️ 4.x 仍在维护（2026-07 更新）但处于大版本过渡。**不建议现在升 5.x**（路由 API 破坏性变更多），建议制定排期评估 |
| uuid | 11.1.1 | 14.0.1 | ⚠️ 落后 3 个大版本，无漏洞；低优先级升级 |
| resend | 6.18.0 | 6.20.0 | 落后 1 个 minor，可跟 |
| socket.io | 4.8.3 | 4.8.3 | ✅ 最新，活跃维护 |
| jsonwebtoken | 9.0.3 | 9.0.3 | ✅ 最新 |
| adm-zip | 0.6.0 | 0.6.0 | ✅ 最新，应用层 Zip Slip 防护充分 |
| multer | 2.2.0 | 2.2.0 | ✅ 最新 |
| helmet / cors / cookie-parser / bcryptjs | 均最新或同 minor | — | ✅ |

### 3.3 许可证合规

- **无 GPL/AGPL 传染性许可**：106 MIT、3 BSD-3、3 ISC、1 Apache-2.0、1 MIT-0、1 Unlicense（`fast-sha256`，公有领域等价）、2 UNKNOWN（`busboy@1.6.0`、`streamsearch@1.1.0`，实际均为 MIT，仅 lock 未标注）。
- 结论：许可证合规风险低；建议将 lock 中 UNKNOWN 两项在内部台账标注实际 MIT 来源。

### 3.4 供应链 / 其他

| 项 | 说明 |
|----|------|
| 依赖锁定 | `package-lock.json` 存在且 lockfileVersion 3；`^` 范围允许 minor 自动升级，建议生产部署用 `npm ci` 锁定 |
| 传递依赖异常 | `@types/node`（26.1.2）出现在 prod 依赖树（socket.io 类型依赖带入），无运行时影响 |
| `node:sqlite` | 数据库引擎为 Node 24+ 内置模块，无第三方驱动，供应链面小（本机实际 Node v26.5.1，满足要求） |
| 无本地可执行依赖 | 编译工具（g++/python/java）走系统 PATH，非 npm 组件，SCA 不覆盖但需运行时版本管理 |

### 3.5 组件安全纵深（代码侧）

- 上传白名单含 `js`（D-L1）是本轮 SCA 之外、与组件无关但建议一并收敛的资产。
- Zip Slip（D-H1）是 adm-zip 使用侧的已知风险点，应用层已部分防御、尚有一处穿越（见 D-H1）。

---

## 四、修复优先级路线图

| 优先级 | 问题 | 预估 |
|--------|------|------|
| P0 | D-H1 ZIP 路径穿越、D-H2 多文件编译参数注入 | 立即修复（各约 20 行） |
| P1 | D-M1 验证码有效期、D-M2 判题 OOM、D-M4 榜单 AC 优先级、D-M5 dependency REPLACE | 本周 |
| P1 | D-M3 submit_answer 未判题（功能缺口，需产品确认） | 与产品对齐 |
| P2 | D-L1~D-L9、S-1~S-6 死代码清理 | 渐进 |
| P3 | S-7~S-15 共享函数抽取、S-16~S-19 响应/错误规范统一、S-20~S-23 日志统一 | 规范迭代 |
| P3 | 组件升级排期（express 5.x 评估、uuid、resend） | 规划 |

---

## 五、修复验证总览（本轮闭环）

> 截至 2026-08-14，第五轮扫描出的缺陷已按 P0→P1→P2→P3 全部处置，并完成 53 项单测与核心链路冒烟回归。以下为本轮修复明细与验证结果。

### 5.1 P0 高危（已修复 + 冒烟验证）

| 项 | 修复 | 验证 |
|----|------|------|
| D-H1 ZIP 路径穿越 | `entryName.replace(/\\/g,'/')` 归一化 + parts 全 `^[\w.-]+$` 白名单 + `assertInsideProblemDir` 写入前前缀断言（源头/纵深双防御） | 构造含 `..\..\evil.in`、`C:\abs.in` 条目 ZIP 上传 → 仅导入正常用例，穿越/绝对路径均被拒绝，磁盘无逃逸文件 |
| D-H2 多文件编译参数注入 | 多文件分支改 `spawnSync('g++', ['-O2','-Wall','-std=c++17','-o',exe,...extraSources,srcFile])` 数组直传；`collectExtraSources` 拒绝含空白/前导 `-`/非 `^[A-Za-z0-9_.-]+$` 文件名 | 直连 executor 编译 main.cpp+helper.cpp（含 `-Wl,--version` 恶意名）→ 编译成功、运行退出码 0、恶意文件被跳过 |

### 5.2 P1 中危（已修复 + 冒烟验证）

| 项 | 修复 | 验证 |
|----|------|------|
| D-M1 验证码有效期 | verify 改 `datetime(expires_at) > datetime('now')`（兼容存量 ISO 格式） | 动览 `expires_at` 十年前 → verify-code 拒绝 400；存储格式确认 ISO `…T…Z` |
| D-M2 判题 OOM | 新增 `readTestdata`（16MB 上限） | 单测通过 |
| D-M3 submit_answer 未判题 | `evaluateTestCases` 开头短路：逐用例 `compareOutput(expected, answer_data)`，沿用 evaluateSimple/evaluateWithGroups 评分 | 提交 `42` → accepted/20 分；提交 `999` → wrong_answer/0 分 |
| D-M4 榜单 AC 被顶 | contests/virtual-contests 都改为「AC 优先 → 同 AC 比 score → 同 score 比 time」 | 构造 AC(100 分/300ms) + WA(100 分/200ms) 同题提交 → 榜单保留 AC，总分 100 |
| D-M5 dependency 字符串误伤 | 读取分组 dependency JSON，`includes` 精确过滤后重写 | 创建含 `112`/`1112` 依赖的组，删除主组后 GET 正常、无非法 JSON |

### 5.3 P2 低危（已修复）

| 项 | 修复 |
|----|------|
| D-L1 | ALLOWED_EXT 移除 js；`/uploads` 加 nosniff + 非图片 attachment（保留 png/jpeg/gif/webp 内联，前端 markdown 图片依赖） |
| D-L2 | 非零退出码 + 输出正确改判 `runtime_error`（2 处） |
| D-L3 | judgeSubmission 记录 wasAccepted/wasCompileError，重评测分去重（`rating` 参数化 `+?`） |
| D-L4 | 榜单 SQL 加 `user_id IN (SELECT user_id FROM contest_participants WHERE contest_id=?)`（表名校验存在） |
| D-L5 | `String(v ?? '').slice(0,1000)` 强转 |
| D-L6 | 删用户前清依赖：discussion_replies/discussions/articles/announcements/problem_sets/submissions 级联删除、problems/contests `created_by=NULL` | 
| D-L7 | testdata 上传 try/finally + cleanupFiles 清理 multer 临时文件 |
| D-L8 | uncaughtException/unhandledRejection `process.exit(1)`；shutdown 5s 强制超时；db.js 新增 `closeDB()` 并在 shutdown 调用 |
| D-L9 | 新增 `recoverInterruptedSubmissions()`（重置 pending 中断态重新入队），server.js listen 前调用 |

### 5.4 P3 规范项（已修复 18 项）

| 项 | 修复 |
|----|------|
| S-1~S-6 | 删 uploads.js `createUploadMiddleware`+不可达分支、db.js `saveDB`、socket.js `getIO`、problems.js `fullProblem`；announcements.js where 简化 |
| S-8 | 抽 `requireCaptcha()` 中间件（login/register 共用） |
| S-9 | `securityHelpers.js` 新增 `banUserAndRevoke()`，ide.js×2 / submissions.js 调用 |
| S-10 | 新建 `utils/validation.js`（isStrongPassword） |
| S-11 | 新建 `utils/tokens.js`（generateAccessToken/generateRefreshToken），users.js 去重 |
| S-13 | server.js 抽 `injectNonce()` |
| S-14 | auth.js 抽 `setRefreshCookie()`（3 处统一） |
| S-16 | `GET /captcha` 404 改标准 `{code,reason,message}`（前端 apiCall catch 兼容） |
| S-17 | categories/languages 重复资源 400→409 + ERR_CONFLICT |
| S-20~S-23 | audit()/submissions reason/problems ZIP err 用 sanitizeLog；judge.js 补 import |
| S-24~S-31 | judge.js 顶部常量（RATING_DELTA_*、MAX_OUTPUT_LOG_CHARS、SPJ_TIMEOUT_MS、MAX_GROUP_EVAL_ITERATIONS、MAX_TESTDATA_BYTES）；ONLINE_TTL_MS 导出；users.js require 提升；server.js 日志合并；virtual-contests 去 async |
| S-32 | loadCaptcha 移入 api.js，login/register 共用 |
| S-34 | 前端 socket 生产日志移除 |

**未做（高风险/需前后端联动，列入技术债）**：S-7（socket JWT 双份认证）、S-12（runSingleTestCase 抽取）、S-15（GET /:id 与 /:id/detail 合并）、S-18（分页 size→limit 字段统一）、S-19（400/403 混用）、S-29（AI 审查并发风格统一）、S-33/S-35/S-36（前端 refresh 去重、stat-card 抽组件、内联事件规范化）。

### 5.5 回归验证矩阵

- **单测**：53 项全通过（scorer/compare/plagiarism）。
- **语法检查**：本轮改动 21 个后端文件 `node --check` 全部通过。
- **冒烟链路**：login（含 captcha 开/关 态）✓、PUT /me 非字符串 signature ✓、重复语言 409 ✓、删用户含依赖清理 ✓、submit_answer 判题对/错 ✓、分组 dependency 清除 ✓、验证码过期拒绝 ✓、排行榜 AC 优先 + 未参赛过滤 ✓、ZIP 穿越拒绝 ✓、多文件 C++ 编译 ✓。
- **事故记录**：期间曾因 `Set-Content -Encoding UTF8` 破坏 judge.js 中文注释（GBK 错读），已 `git checkout` 恢复后重做全部编辑并通过验证。

---

## 六、总结

- **代码缺陷**：2 高危（ZIP 穿越、编译参数注入）需优先处置；5 中危含 1 个真实有效期漏洞（验证码）与 1 个功能未实现（submit_answer）；9 低危以资源清理/容错为主。上一轮全部修复项无回退。
- **代码规范**：36 项，以死代码（6 处）、重复逻辑（9 组）、日志脱敏缺口（4 类）最有价值，均可渐进修复。
- **组件安全**：`npm audit` 0 漏洞、许可合规、无停维护关键组件；剩余为版本健康度（express 5.x / uuid）规划项。

---

## 七、第五轮修复重扫验证（Fix Verification）

> 扫描方式：比对 commit `c3ab2db` 全部改动 + 全量 `node --check` + 单测复跑。
> 结论：D-H/D-M 全部 7 项及 D-L 8 项修复**有效**；1 处低危修复**无效**（D-L3 Rating 去重），并发现 1 处**重构引入的新回归**（验证码刷新失效）。

### 7.1 已确认修复有效

| 项 | 验证结果 | 证据 |
|----|----------|------|
| D-H1 ZIP 路径穿越 | ✅ **有效** | `entryPath` 归一化 `\→/`；parts 白名单 `[\w.-]+`；写入前 `assertInsideProblemDir` 前缀断言三重防护（problems.js:353-357,437-452） |
| D-H2 多文件编译参数注入 | ✅ **有效** | 多文件 C++ 改 `spawnSync('g++', ['-O2','-Wall','-std=c++17','-o',exe, ...extra, src])` 数组直传；`collectExtraSources` 过滤 `前导-`/非 `[\w.-]` 文件名（executor.js:122-189） |
| D-M1 验证码有效期 | ✅ **有效** | `datetime(expires_at) > datetime('now')` 归一化 ISO 与空格两格式比对（email.js:54） |
| D-M2 判题 OOM | ✅ **有效** | `readTestdata` 16MB 单文件上限，异常抛 `system_error` 落库（judge.js:59-71） |
| D-M3 submit_answer | ✅ **有效** | judge.js:118-151 短路：逐用例 `compareOutput(expected, answer_data)`，沿用分组/简单评分 |
| D-M4 榜单 AC 优先 | ✅ **有效** | contests.js:271-283 与 virtual-contests.js:115-127 均改为 AC 优先→score→time |
| D-M5 dependency REPLACE | ✅ **有效** | 改 JSON.parse→includes 过滤→重新 stringify（problems.js:620-635） |
| D-L1 uploads js/nosniff | ✅ **有效** | ALLOWED_EXT 移除 js；`/uploads` 静态服务加 `X-Content-Type-Options: nosniff` + 非图片 attachment（server.js:167-175） |
| D-L2 非零退出码 | ✅ **有效** | 两处改 `else if (result.exitCode !== 0) status='runtime_error'`（judge.js:233-234,271-272） |
| D-L4 未参赛者入榜 | ✅ **有效** | 榜单 SQL 追加 `s.user_id IN (SELECT user_id FROM contest_participants WHERE contest_id=?)` |
| D-L5 signature 崩溃 | ✅ **有效** | `String(v ?? '')` 先强转 |
| D-L6 删用户 FK | ✅ **有效（注意数据丢失）** | 删前级联清 discussion_replies/discussions/articles/announcements/problem_sets/submissions + problems/contests 置 NULL。⚠️ 该实现会**永久删除用户全部提交/文章等历史**，属数据保留策略决策，需产品确认 |
| D-L7 multer 残留 | ✅ **有效** | `cleanupFiles()` + try/finally |
| D-L8 退出处理 | ✅ **有效** | uncaughtException/unhandledRejection `exit(1)`；shutdown 5s 强退；`db.closeDB()` |
| D-L9 队列恢复 | ✅ **有效** | `recoverInterruptedSubmissions()` 重置中断态重新入队，listen 前调用 |

### 7.2 ✅ D-L3 Rating 重判去重 —— 修复有效

**方案**：重判路由在改状态前捕获原始状态，随任务传入判题池（`submissions.js:292`），判题时据此判定是否已 AC/CE。

```js
// submissions.js
const prevStatus = submission.status;                 // 改状态前捕获
db.prepare("UPDATE submissions SET status='pending_rejudge',...").run(submission.id);
enqueueSubmission(submission.id, prevStatus);         // 原始状态随任务传入

// judge.js:607 rejudgePrevStatus Map 记录；judge.js:528-530 读取
const prevStatus = rejudgePrevStatus.get(submissionId) || submission.status;
const wasAccepted = prevStatus === 'accepted';
const wasCompileError = prevStatus === 'compile_error';
// runOne finally 中 delete，Map 无残留（judge.js:616-618）
```

**验证**：`wasAccepted/wasCompileError` 现能正确取到重判前的真实状态，`firstAccepted` 判断生效，重判 AC 不再重复 +10、重判 CE 不再重复 -2。Map 有界（任务完成即 delete）。

### 7.3 ✅ 验证码点击刷新回归 —— 修复有效

**位置**：`api.js:465-486` 改为回调式 `loadCaptcha(onLoaded)`，`login.html:52-55,82-85` / `register.html:79-82,145-148` 传入同步回调。

**时序（修复后）**：
1. 初始加载与点击刷新均调用 `loadCaptcha(onLoaded)`；
2. 内部 `container.onclick = () => loadCaptcha(onLoaded)` 回调透传；
3. 每次成功加载后 `onLoaded(state)` 同步回写 `captchaId`/`captchaEnabled`。

→ 点击刷新后 `captchaId` 同步为新 id，登录/注册提交不再错配，`verifyCaptcha` 命中正确会话。

### 7.4 ✅ 其他观察项跟进

| 项 | 状态 |
|----|------|
| `ide.js:25` 缩进 | ✅ 已修复 |
| `judge.js:227-234` 缩进 | ✅ 已修复 |
| S-10 密码校验三份实现 | ✅ **已收敛**：`users.js` reset-password 与 create-user 均改调 `isStrongPassword()`（`utils/validation.js`），行为统一（create-user 强度校验对齐注册接口） |
| 组件（SCA） | `npm audit` 仍 0 漏洞，依赖未变 |

### 7.5 验证方法

- 语法：全部改动文件 `node --check` 通过。
- 单测：`scorer/compare/ratelimit/plagiarism` 共 **56 项全通过**（`security.test.js`/`reset-login.test.js` 为需先 `npm start` 起的集成测试，属既有前置条件）。
- 上一轮唯一"未修复"的 D-L3 与唯一"新回归"验证码问题，本轮均已闭环验证。
