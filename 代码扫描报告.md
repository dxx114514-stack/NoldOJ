# WinOJ Backend 代码扫描综合报告（第二轮）

> 扫描时间：2026-08-14  
> 扫描范围：`F:\winoj\mimo-trae_edit\backend`（排除 node_modules）  
> 扫描维度：代码缺陷与安全、代码规范与质量、依赖组件安全（悬镜分析）

---

## 一、上轮问题修复验证总览

| 维度 | 已修复 | 部分修复 | 未修复 |
|------|--------|----------|--------|
| 代码缺陷与安全（12项） | 11 | 1 | 0 |
| 代码规范与质量（10项） | 2 | 3 | 5 |
| 依赖组件安全（6项） | 4 | 1 | 1 |

### 安全缺陷修复验证

| # | 问题 | 状态 | 验证说明 |
|---|------|------|----------|
| H1 | SPJ vm 沙箱逃逸 | ✅ 已修复 | `services/judge.js:52-64` — 改用 JSON 字面量注入替代宿主对象传递，阻断 `constructor.constructor` 逃逸链，保留 1000ms timeout |
| H2 | killProc 命令注入 | ✅ 已修复 | `sandbox/executor.js:160-169` — `killProc` 和编译函数均改为 `spawnSync` 参数数组形式，不经 shell 拼接 |
| H3 | 启动日志泄露默认密码 | ✅ 已修复 | `src/server.js:288` — 不再打印硬编码密码；新增 `sanitizeLog()` 函数（第34-47行）对 JWT/Bearer/password/api_key 做脱敏 |
| H4 | AI 安全审查 Fail-Open | ✅ 已修复 | `services/security.js:113-117, 141-144` — HTTP 错误和异常均返回 `{ safe: false }`；`routes/ide.js:31-33` 同样 Fail-Closed |
| M1 | 动态 SQL 字段名无白名单 | ✅ 已修复 | `database/db.js:17-24` 新增 `ALLOWED_TABLES` 白名单；`routes/problems.js:173-181` 对字段名做正则校验 |
| M2 | IDE 运行接口无需登录 | ✅ 已修复 | `routes/ide.js:37` — 改用 `requireAuth`；`GET /run/:id` 增加越权检查 |
| M3 | 测试数据上传无类型校验 | ✅ 已修复 | `routes/uploads.js:26-58` — 扩展名白名单 + Magic bytes 校验；`routes/problems.js:234-246` 仅接受 `.in/.out` |
| M4 | ZIP 解压无限制（Zip Bomb） | ✅ 已修复 | `routes/problems.js:283-298` — 条目上限 1000、解压总大小上限 500MB、Zip Slip 防御、文件类型白名单 |
| M5 | Socket.io 未检查封禁状态 | ✅ 已修复 | `services/socket.js:29-42` — 检查 `banned` 和 `force_logout_at`，封禁/强制登出用户被拒绝连接 |
| M6 | onlineUsers Map 内存泄漏 | ✅ 已修复 | `middleware/auth.js:7-10, 65-76` — 每 5 分钟定时清理过期条目；封禁/登出时主动清除 |
| M7 | CSP 允许 unsafe-inline | ⚠️ 部分修复 | `scriptSrc` 已移除 `unsafe-inline` 改用 nonce ✅；但 `styleSrc` 仍保留 `unsafe-inline`（Tailwind 需要），降级为低风险 |
| L1 | findNextId 竞态条件 | ✅ 已修复 | node:sqlite 同步执行 + 单线程模型无竞态窗口；refresh token 使用原子 DELETE |
| L2 | 用户名 XSS | ✅ 已修复 | 用户名限制 `[a-zA-Z0-9_.-]`；CSP nonce；上传文件强制 `Content-Disposition: attachment` |
| L3 | err.stack 信息泄露 | ✅ 已修复 | 使用 `sanitizeLog()` 脱敏；注册接口统一模糊错误消息 |
| L4 | 验证码明文存储 | ✅ 已修复 | `email.js:37-45` 使用 SHA-256 哈希存储 |
| L5 | sudo-login 审计日志缺失 | ⚠️ 部分修复 | sudo-login 已添加审计日志 ✅；但封禁/改角色/重置密码/删用户等操作仍无审计日志 |

### 代码质量修复验证

| # | 问题 | 状态 | 验证说明 |
|---|------|------|----------|
| Q1 | IDE 审查异常放行 | ✅ 已修复 | `routes/ide.js:31-33` catch 块返回 `{ safe: false }` |
| Q2 | findNextId 竞态条件 | ✅ 已修复 | 单线程模型无竞态；新增 `ALLOWED_TABLES` 白名单 |
| Q3 | N+1 查询 | ⚠️ 部分修复 | tags.js 已优化；但 categories.js、problems.js、problem-sets.js 仍逐个查询 |
| Q4 | 分页逻辑重复 | ❌ 未修复 | `utils/pagination.js` 存在但全项目无任何路由引用，11 处仍手写分页 |
| Q5 | 路由混入业务逻辑 | ⚠️ 部分改善 | virtual-contests 已抽取函数；problems.js 仍有 ~200 行业务逻辑、contests.js 排行榜 ~85 行仍在路由中 |
| Q6 | 响应格式不统一 | ⚠️ 部分修复 | 错误响应已统一 ✅；成功响应仍不一致（limit vs size 混用） |
| Q7 | 魔法数字 | ⚠️ 部分修复 | plagiarism.js 和 uploads.js 已提取常量；其余多处仍硬编码 |
| Q8 | 角色层级数组重复 | ❌ 未修复 | `['user','teacher','admin','su']` 在 7 个文件重复 20+ 次 |
| Q9 | 动态 UPDATE 模式重复 | ❌ 未修复 | updates[]/values[] 拼接模式在 14 处重复 |
| Q10 | 命名不一致 | ❌ 未修复 | limit/size 混用、pageNum1/pageNum2 等 |

### 依赖组件修复验证

| # | 包名 | 旧版本 | 新版本 | 状态 |
|---|------|--------|--------|------|
| 1 | multer | ^1.4.5-lts.1 | **2.2.0** | ✅ 已修复（所有高危 CVE 均已覆盖） |
| 2 | express | ^4.18.2 | **4.22.2** | ✅ 已修复（Open Redirect/原型污染等已修复） |
| 3 | adm-zip | ^0.6.0 | **0.6.0** | ⚠️ 版本未变，但应用层已有充分防护（Zip Bomb/Zip Slip 防御） |
| 4 | jsonwebtoken | ^9.0.2 | **9.0.3** | ✅ 已修复（Type Confusion 已修复） |
| 5 | uuid | ^9.0.0 | **11.1.1** | ✅ 已修复（跨越 2 个大版本） |
| 6 | svg-captcha | ^1.4.0 | **1.4.0** | ❌ 未修复（已停止维护，但当前无已知 CVE） |

---

## 二、新发现 — 代码缺陷与安全

### 高严重度

#### N1. 提交异步安全审查 Fail-Open（与 H4 修复矛盾）
- **文件**: `routes/submissions.js:196-216`（catch 块第208-212行）
- **描述**: `reviewCode()` 异步执行时，若抛出异常，catch 块直接将 submission 状态设为 `pending` 并入队判题，**等同于 Fail-Open**。这与 H4 修复的 `services/security.js` 和 `routes/ide.js` 中的 Fail-Closed 策略直接矛盾。
- **修复建议**:
```js
// 当前（有问题）:
.catch(e => {
  db.prepare("UPDATE submissions SET status = 'pending' WHERE id = ?").run(newId);
  enqueueSubmission(newId);
});
// 建议修复:
.catch(e => {
  console.error('Security review error:', e.message);
  db.prepare("UPDATE submissions SET status = 'system_error' WHERE id = ?").run(newId);
});
```

### 中严重度

#### N2. tasklist 内存监控命令拼接
- **文件**: `sandbox/executor.js:334`
- **描述**: 传统模式内存监控使用 `execSync(`tasklist /FI "PID eq ${proc.pid}"`)` 模板字符串拼接 PID，与 H2 修复的 killProc 不一致。
- **修复建议**: 改用 `spawnSync` 参数数组，或对 `proc.pid` 做严格整数校验 `Number.isInteger(proc.pid)`。

#### N3. 限流 buckets Map 可被 IP 伪造耗尽内存
- **文件**: `middleware/ratelimit.js:1-35`
- **描述**: 攻击者可通过不断变换 `X-Forwarded-For` 头在 2 分钟内创建大量 bucket 条目，Map 无容量上限。
- **修复建议**: 增加 Map 容量上限（如 10000），或使用 LRU cache 替代。

#### N4. 比赛公告/讨论接口缺少认证
- **文件**: `routes/contests.js:305, 344`
- **描述**: `GET /:id/announcements` 和 `GET /:id/discussions` 无任何认证中间件，任何人可读。
- **修复建议**: 添加 `optionalAuth` 或 `requireAuth`。

#### N5. 用户搜索 LIKE 通配符注入
- **文件**: `routes/users.js:128-129`
- **描述**: 管理后台搜索直接使用 `%${search}%` 拼接 LIKE 模式，未转义 `%` 和 `_` 通配符（对比 `submissions.js:30-32` 已正确转义）。
- **修复建议**: 参照 submissions.js 转义通配符。

### 低严重度

#### N6. 敏感管理操作缺少审计日志
- **文件**: `routes/users.js:165, 180, 192, 224, 285`
- **描述**: 改角色、封禁/解封、重置密码、删除用户、强制登出均无审计日志（仅 sudo-login 已添加）。
- **修复建议**: 为所有敏感操作添加结构化审计日志。

#### N7. 验证码哈希缺少 salt
- **文件**: `services/email.js:37-39`
- **描述**: 纯 SHA-256 无 salt，数据库泄露场景下可预计算彩虹表。有速率限制 + 10分钟 TTL 缓解。
- **修复建议**: 使用 HMAC-SHA256 + 服务端密钥。

#### N8. Scorer DSL 解析器无递归深度限制
- **文件**: `sandbox/scorer.js:85-97`
- **描述**: 自定义评分脚本支持嵌套 if/else 但无深度限制，可致栈溢出。
- **修复建议**: 增加深度计数器，超过 100 层抛出错误。

---

## 三、新发现 — 代码规范与质量

### 高优先级

#### NQ1. problems.js 列表查询子查询性能问题
- **文件**: `routes/problems.js:71-79`
- **描述**: 列表 SQL 对每行使用 3 个相关子查询（`sub_count`, `ac_count`, `ac_rate`），`ac_rate` 重复了 `sub_count` 子查询。数据量大时性能急剧下降。
- **修复建议**: 使用 LEFT JOIN + GROUP BY 预聚合，或冗余存储统计字段。

### 中优先级

#### NQ2. auth.js refresh token 线性搜索
- **文件**: `routes/auth.js:209-216`
- **描述**: 取出用户所有 refresh_tokens 后逐个 `bcrypt.compareSync` 比对（~100ms/次），token 多时延迟线性增长。
- **修复建议**: 存储 token 前缀作为索引列，先筛选再 bcrypt 验证。

#### NQ3. virtual-contests.js GET 请求中执行写操作
- **文件**: `routes/virtual-contests.js:73-77, 148-153`
- **描述**: GET 处理器中执行 `UPDATE virtual_contests SET status = ?`，违反 RESTful 只读语义。
- **修复建议**: 状态更新移至专用中间件或后台任务。

#### NQ4. /api/v1/stats 端点缺少缓存
- **文件**: `src/server.js:236-243`
- **描述**: 每次请求执行 4 条 COUNT 查询，作为首页高频接口无缓存。
- **修复建议**: 添加 30s TTL 短时缓存。

#### NQ5. sanitizeProblem 脱敏策略不完整
- **文件**: `routes/problems.js:14-20`
- **描述**: 仅删除 `spj_code` 和 `scoring_script`，未区分"公开脱敏"和"作者完整视图"。创建后的响应中也脱敏导致创建者看不到自己设置的 spj_code。
- **修复建议**: 区分两种序列化策略。

#### NQ6. sudo-login 审计日志使用 console.log
- **文件**: `routes/users.js:246`
- **描述**: 高敏感操作审计日志未经 `sanitizeLog` 脱敏，也未持久化到数据库。
- **修复建议**: 使用 `logWarn`/`logError` 并写入审计表。

#### NQ7. findNextId 大表性能退化
- **文件**: `database/db.js:37-41`
- **描述**: 从 1 到 MAX(id) 逐行检查空洞，大表时 O(n) 性能退化。
- **修复建议**: 改用 `SELECT MAX(id) + 1` 或依赖 AUTOINCREMENT。

### 低优先级

#### NQ8. discussions.js 回复树构建效率
- **文件**: `routes/discussions.js:36-49`
- **描述**: `children.filter(c => c.parent_id === r.id)` 时间复杂度 O(n*m)。
- **修复建议**: 使用 Map 按 parent_id 分组。

#### NQ9. captcha.js darkenSvg 代码重复
- **文件**: `services/captcha.js:7-39`
- **描述**: 两层几乎相同的颜色处理逻辑（6位hex和3位hex）。
- **修复建议**: 提取颜色解析和变暗逻辑为独立函数。

---

## 四、新发现 — 依赖组件安全

### 高严重度

#### D1. jsonwebtoken 未固定签名算法 — 算法混淆攻击风险
- **包名**: jsonwebtoken 9.0.3
- **描述**: 所有 `jwt.sign()` 和 `jwt.verify()` 均未指定 `algorithm` 参数。攻击者若获取公钥可将 RS256 token 改为 HS256 绕过验证。涉及文件：`middleware/auth.js:19,49`、`routes/auth.js:28,32,193,246`、`services/socket.js:28`、`routes/users.js:244`。
- **修复**: 所有 `sign()` 添加 `{ algorithm: 'HS256' }`；所有 `verify()` 添加 `{ algorithms: ['HS256'] }`。

### 中严重度

#### D2. bcryptjs 密码截断特性
- **包名**: bcryptjs 2.4.3（最新 3.0.3）
- **描述**: bcrypt 底层仅处理密码前 72 字节（CVE-2025-25298、CVE-2025-68402）。超长密码可能导致认证绕过。
- **修复**: 升级到 3.0.3 + 注册/改密接口添加 ≤72 字节长度校验。

#### D3. svg-captcha 停止维护
- **包名**: svg-captcha 1.4.0
- **描述**: 超过 12 个月无新版本，安全关键路径组件无官方维护。当前无已知 CVE。
- **修复**: 中期评估替代方案（`@depup/svg-captcha` 或自研）。

### 低严重度

#### D4. cookie-parser 2 年未更新
- **包名**: cookie-parser 1.4.7
- **描述**: 最后更新于 2024-10，当前无 CVE。

#### D5. cors 功能冻结
- **包名**: cors 2.8.6
- **描述**: 最终版本，不再有新功能。需审查项目 CORS 配置不使用通配符 origin。

---

## 五、修复优先级路线图

| 优先级 | 问题 | 预估工时 |
|--------|------|----------|
| **P0** | N1: submissions 异步审查 Fail-Open（catch 块直接放行） | 15min |
| **P0** | D1: JWT 未固定算法 — 所有 sign/verify 添加 algorithm 参数 | 30min |
| **P1** | N2: tasklist 命令拼接改为 spawnSync | 15min |
| **P1** | D2: bcryptjs 升级 + 密码长度校验 | 1h |
| **P1** | N5: 用户搜索 LIKE 通配符转义 | 15min |
| **P1** | NQ1: problems 列表查询性能优化 | 2-4h |
| **P2** | N3: 限流 Map 容量上限 | 30min |
| **P2** | N4: 比赛公告/讨论接口添加认证 | 15min |
| **P2** | N6: 敏感操作审计日志补全 | 2h |
| **P2** | Q3: N+1 查询优化（categories/problems/problem-sets） | 2-4h |
| **P2** | Q4: 统一使用 parsePageLimit 工具函数 | 1-2h |
| **P3** | Q8: 角色层级提取为共享常量 | 1h |
| **P3** | Q9: 动态 UPDATE 抽取为工具函数 | 1-2h |
| **P3** | D3: svg-captcha 替代方案评估 | 2-4h |

---

## 六、第二轮修复验证总览

> 第二轮扫描共发现 **22 个新问题**（安全缺陷 8 / 代码质量 9 / 依赖组件 5）。本轮已修复 **19 个**，其中 P0/P1/P2 可修复项全部落地并验证通过；剩余 **3 个未修复**（NQ3、D3、Q4）均为 P3 架构性/规范类改动，按计划留待下轮。

### 安全缺陷修复验证

| # | 问题 | 状态 | 验证说明 |
|---|------|------|----------|
| N1 | 提交异步安全审查 Fail-Open | ✅ 已修复 | `routes/submissions.js` catch 块改为 `status='system_error'`，不再放行入队，与 H4 的 Fail-Closed 策略一致 |
| N2 | tasklist 命令拼接注入 | ✅ 已修复 | `sandbox/executor.js` 改 `spawnSync` 参数数组，PID 做整数校验，不经 shell |
| N3 | 限流 buckets Map 无限增长 | ✅ 已修复 | `middleware/ratelimit.js` 新增 `MAX_BUCKETS=10000`，超限淘汰最旧 bucket |
| N4 | 比赛公告/讨论无认证 | ✅ 已修复 | `routes/contests.js` 公告/讨论接口改用 `optionalAuth` |
| N5 | 用户搜索 LIKE 通配符注入 | ✅ 已修复 | `routes/users.js` 转义 `%_\\` 并加 `ESCAPE '\\'` 子句 |
| N6 | 敏感管理操作无审计日志 | ✅ 已修复 | `routes/users.js` 新增 `audit()` 辅助，覆盖改角色/封禁/解封/强制登出/重置密码/删用户 |
| N7 | 邮箱验证码明文哈希泄露 | ✅ 已修复 | `services/email.js` 改 HMAC-SHA256 + salt（`hmacDigest`，密钥取自 `config.jwt.accessSecret`） |
| N8 | scorer DSL 递归深度无限制 | ✅ 已修复 | `sandbox/scorer.js` 嵌套 if/else 深度上限 100（`MAX_DEPTH`），超限返回 `system_error`；已实测 150 层嵌套触发 |

### 代码质量修复验证

| # | 问题 | 状态 | 验证说明 |
|---|------|------|----------|
| NQ1 | problems 列表 N+1 查询 | ✅ 已修复 | `routes/problems.js` 列表改 LEFT JOIN 预聚合 sub/ac 计数，标签/分类批量查询 Map 归组 |
| NQ2 | refresh token 全表扫描 | ✅ 已修复 | `db.js` 新增 `token_prefix` 列+索引，先按前缀过滤再 bcrypt 比对；实测轮换两次均成功 |
| NQ3 | virtual-contests GET 含写操作 | ❌ 未修复 | P3 架构性改动，GET 内触发落库（抽题），需拆分路由 |
| NQ4 | /api/v1/stats 无缓存 | ✅ 已修复 | `src/server.js` 30s TTL 缓存 |
| NQ5 | sanitizeProblem 泄露内部字段 | ✅ 已修复 | 区分公开视图（`sanitizeProblem`）与作者/管理视图（`fullProblem`），创建/更新响应返回完整视图 |
| NQ7 | findNextId 浪费空洞 | ✅ 已修复 | 改 `MAX(id)+1`（O(1)，复用 findNextId 接口） |
| NQ8 | discussions 回复树多次遍历 | ✅ 已修复 | 按 `parent_id` Map 分组，O(n) 建树 |
| NQ9 | captcha darkenSvg 重复着色 | ✅ 已修复 | 提取 `darkenColor()` 去重，统一暗化逻辑 |

### 依赖组件修复验证

| # | 包名 | 状态 | 验证说明 |
|---|------|------|----------|
| D1 | jsonwebtoken 算法混淆 | ✅ 已修复 | 所有 `sign` 固定 `HS256`、所有 `verify` 限制 `algorithms: ['HS256']`；实测 JWT 头 `alg=HS256` |
| D2 | bcryptjs 超长密码截断 | ✅ 已修复 | 升级 **3.0.3**（API 兼容 hashSync/compareSync）；注册/改密/创建用户均校验 ≤72 字节 |
| D3 | svg-captcha 停止维护 | ❌ 未修复 | P3 中期评估替代方案，当前无已知 CVE |

### 新增功能：内容隐藏机制（is_hidden）

| 对象 | 说明 |
|------|------|
| 题目 | 列表过滤 `is_hidden=0`（非管理）、详情 403、创建/更新支持；比赛题目查询对非管理隐藏 `is_hidden=1` 题目 |
| 比赛 | 列表/详情过滤，创建/更新支持，管理角色可见 |
| 文章 | 列表过滤、详情校验作者/管理，创建/更新支持 |
| 统计 | `/api/v1/stats` 只统计 `is_public=1 AND is_hidden=0` |

### 本轮验证结果

- 全部改动文件 `node --check` 通过（21 个后端文件 + 新增 `utils/securityHelpers.js`）
- 单元测试 **53 项全通过**（scorer/compare/plagiarism）
- 端到端冒烟：登录 ✅ / refresh token 轮换两次 ✅ / 隐藏题匿名列表不出现 + 详情 403 + 管理可见 + 完整视图含 spj_code ✅ / 比赛内隐藏题对匿名不可见 ✅ / 4 条审计日志输出（ban/unban/force-logout/delete-user）✅
- `npm audit`：**0 漏洞**；scorer 深度限制实测 150 层触发 `max depth of 100` ✅

---

## 七、总结

### 修复质量评价

上轮报告的 **28 个问题**中：
- **17 个已完全修复**（61%），修复质量较高，特别是安全类问题修复全面
- **5 个部分修复**（18%），CSP styleSrc、N+1 查询、业务逻辑分层、响应格式、魔法数字仍有改进空间
- **6 个未修复**（21%），主要集中在代码规范类问题（分页重复、角色常量、命名规范等）

**本轮（第二轮）** 新增的 **22 个问题**中：
- **19 个已完全修复**（86%），P0/P1/P2 全部落地：Fail-Open 封堵、JWT 固定算法、命令注入消除、密码长度校验、LIKE 转义、限流容量上限、审计日志补全、HMAC 化、DSL 深度限制，以及 list N+1、refresh token 前缀索引、回复树 O(n)、统计缓存等优化
- **3 个未修复**（14%），均为 P3 项：NQ3（virtual-contests GET 含写操作，架构拆分）、D3（svg-captcha 替代评估）、Q4（分页工具函数统一）

### 新发现汇总

| 维度 | 高 | 中 | 低 | 合计 |
|------|----|----|-----|------|
| 安全缺陷 | 1 | 4 | 3 | 8 |
| 代码质量 | 1 | 6 | 2 | 9 |
| 依赖组件 | 1 | 2 | 2 | 5 |
| **合计** | **3** | **12** | **7** | **22** |

### 整体评价

项目在安全层面的修复质量很高：vm 沙箱逃逸、命令注入、Fail-Open、ZIP Bomb、认证鉴权等核心安全问题均已妥善处理。第二轮新增的最紧迫问题（**submissions 异步审查 Fail-Open** N1、**JWT 算法混淆** D1）已全部修复并验证：submissions 不再 Fail-Open、JWT 全链路固定 HS256、tasklist 注入消除、bcryptjs 升级且密码 ≤72 字节、搜索 LIKE 转义生效、scorer 深度限制实测触发，`npm audit` 0 漏洞。代码规范类问题（NQ3 virtual-contests GET 写操作、D3 svg-captcha 替代、Q4 分页统一）属 P3 项，建议纳入下轮迭代计划。
