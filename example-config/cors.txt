# CORS 白名单配置
# 说明：
#  - 每行一个允许跨域访问的源地址（去掉末尾 /），以 # 开头为注释行。
#  - 默认内置：http://localhost、http://localhost:3000、http://127.0.0.1、http://127.0.0.1:3000
#  - 本地局域网访问时，请把所在机器的地址加入，例如 http://192.168.1.100:3000
#  - 若该文件缺失，仅允许上述内置本机地址。

# http://192.168.1.100:3000
# https://oj.example.com